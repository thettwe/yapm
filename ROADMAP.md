# yapm — Roadmap

Scope decisions from the v1 scoping session (2026-07-23). Each item becomes an OpenSpec change (`openspec/changes/`) when its turn comes — proposals are written just-in-time, not upfront.

## V1 scope (locked)

**Shape**: one workspace per instance → teams → issues. Roles: admin / member / viewer (viewer free and unlimited).
**Tracking**: issues (status, priority, labels, assignee, TipTap description, comments) · cycles with auto-rollover · triage inbox · lightweight projects (name, lead, target date, membership, computed progress).
**Surfaces**: keyboard-first list · board · roadmap (timeline of projects) · command palette · search/filtering.
**Wedge proof**: GitHub App — branch/PR ↔ issue linking, PR state drives issue status **for teams that opt in** (off by default; the `//` divergence break in the reality track is what happens otherwise), PR + checks visible on the issue.

**Opinionated defaults (not asked, decided per philosophy):** fixed status categories (Backlog / Todo / In Progress / In Review / Done / Canceled); estimates deferred; sub-issues deferred; notifications = in-app inbox + email for mentions/assignments.

## Change sequence

| # | Change | Contents | Status |
|---|--------|----------|--------|
| 1 | `foundation` | Monorepo, 3-container compose, CI, dev loop, Zero walking skeleton | ✅ built & archived |
| 2 | `workspace-auth` | Workspace, users, teams, roles, better-auth (email/password, GitHub OAuth, OIDC — free), invites, row-level sync permissions | ✅ built & archived |
| M1 | `design-system` | Tokenized theme system: Warm/Focused/Editorial presets + accent customization + per-user synced preference; strictly-tokenized component library; issue-row primitive with reserved reality-strip + divergence slots | ✅ built & archived |
| 3 | `issue-core` | Issues + statuses/priorities/labels/comments, keyboard-first list, command palette, filtering. **Reality strip + divergence flag** in the row (layout now, git data via connectors); filter architecture designed for derived delivery state | ✅ built & archived |
| 4 | `board-view` | Kanban board grouped by status; dnd-kit + fractional-index single-write moves; keyboard + command-palette move | ✅ built & archived (PR #1) |
| 5 | `cycles` | Time-boxed iterations (team-scoped, per-team number, status upcoming/active/completed), issue↔cycle assignment, auto-rollover of unfinished work on completion via a deliberate action and a pg-boss scheduler, cycle progress view, cycle grouping + filtering in the list | ✅ built & archived (PR #2) |
| 6 | `triage` | Triage inbox for incoming/unsorted issues: an orthogonal `needs_triage` flag (not a seventh status), team-scoped inbox query, keyboard-first accept/decline/route, command-palette actions | ✅ built & archived (PR #3) |
| 7 | `projects-roadmap` | Lightweight workspace-level projects (name, lead, target date, status, computed progress) + nullable issue→project reference, project view, roadmap timeline, project grouping + filtering in the list | ✅ built & archived (PR #4) |
| 8 | `connectors` | **First-party connector framework** + GitHub as its first connector: webhook ingestion (pg-boss, serialized per install), PR/CI/deploy → work-graph edges, branch/PR ↔ issue linking (linking only — driving issue status is change 14). Shared encrypted-secrets/config surface. Gated on the user registering a GitHub App. GitLab etc. = later connectors | ✅ built & archived (PR #5) |
| 9 | `ai` | **BYO-key, work-graph-native AI**: provider-agnostic gateway (Anthropic/Gemini/OpenAI), toggle + configure per workspace, agents that read via the same synced queries and act via the same mutators + permissions. Reuses the connector secrets/config surface. Built after the graph exists so the AI is differentiated by the data, not the model. Ships the gateway + agent-as-actor model + the AI-over-work-graph substrate, validated by one flagship: a team-internal, read-only, evidence-linked **cycle digest** pre-computed at cycle close (the governed PM-facing digest + AI-facilitated retro are later changes) | ✅ built & archived (PR #6) |
| 10 | `retro-board` | **Data-seeded retrospective**: the differentiator is the seed, not the board — a retro auto-opened at cycle close whose gather-data phase is pre-filled from the work graph (Delivered from cycles alone, Flow when a connector is present), anonymity guaranteed at the storage layer via a server-only card→author table absent from the Zero schema, a server-enforced six-phase machine, and actions converted into real numbered issues in the next cycle through the shared create path. No AI — the AI-facilitated retro is a later change on the change-9 substrate | ✅ built & archived (PR #8) |
| M2 | `zero-reconnect` | **Maintenance (P1).** Sync survives an idle tab: the query/mutate endpoints answer a presented-but-invalid credential with `401` instead of a `200` that strands the client group; the client re-mints and reconnects from every non-terminal broken state under jittered backoff (1s → 30s cap); the sync token refreshes proactively; and the connection pill becomes a tokenized, keyboard-operable, announced reconnecting state. Closes the CI gap that let it ship — reconnection is now covered end to end | ✅ built & archived (PR #7) |
| 11 | `notifications` | **In-app inbox + email, with the fan-out on the authoritative pass**: per-recipient `notification` rows written only in the server mutator override (never on the client, never on rebase), idempotent under a deterministic natural key, for assignment (including triage routing) and comments on issues you are involved in; a self-scoped synced inbox with **no admin bypass**, a keyboard-first `/inbox` with optimistic read/unread; batched, debounced, read-suppressed email on the existing pg-boss scheduler, cleanly disabled when no transport is set. Ships the shared **provider-neutral mailer** — an SMTP transport and a Resend-over-HTTPS one for hosts that block outbound SMTP ports — plus `packages/email` (react-email templates rendered once, above the transport), closing `invitations`' one unmet spec scenario and lighting up the `SMTP_URL` env var that had shipped unused since `workspace-auth` | ✅ built & archived (PR #9) |
| 12 | `mentions` | `@`-mentions in TipTap descriptions and comments (follows `notifications`): `@tiptap/extension-mention` + `@tiptap/suggestion` at **exactly 3.28.0** (the whole TipTap graph pinned together — the new packages' peer ranges are exact, and a split resolution duplicates `prosemirror-model` and throws at *runtime*) with a bespoke keyboard-first listbox over already-synced users — **no network on the hot path**; stored as an id-referencing node so renames propagate and labels cannot spoof; eligibility = the same `teamScoped` read predicate (admin bypass included), enforced server-side at event production, so mentioning someone who cannot read the issue produces nothing — and the list says *why* rather than going quiet. Diff-on-edit against the previously stored document, so an edit notifies only new mentions and a re-save notifies nobody. **Scope grew after the H7 answer:** a mention **auto-subscribes** the mentioned person, which cannot be derived from `notification` rows (30-day retention would expire it silently) — so a new durable `issue_subscription` table, migration **`0014_mentions`**, a Zero schema change, and a sticky per-issue unfollow ship with it. Subscription activity is in-app only (the new `mention` kind is actionable and emailed; subscriber comment notifications reuse the ambient `issue_commented` kind through the *public* `recordNotifications` seam, deduplicated against the involvement producer by the composite primary key). Also fixes a live defect: Escape inside the rich-text editor discarded the whole draft | ✅ built & archived (PR #10, #11) |
| 13 | `search` | **Instant-then-complete search**: a shared ranking core in `packages/schema`, a local pass over already-synced rows that answers on the keystroke with no network, and a Postgres full-text pass (server-only `search_document` sidecar + GIN expression index in migration **`0015_search`**, no new container and **no `CREATE EXTENSION`** — `pg_trgm` refused, since some managed-Postgres hosts cannot grant it) that extends it with comment bodies and other teams' issues under the same team-scoped predicate as the synced queries. **The maintainer answered H10 against the original plan:** the index is *not* maintained in the server-mutator wrapper — it is a pg-boss job on the existing scheduler, accepting ~10s of staleness so an issue-title edit stays inside the sub-100ms budget (deletion is the one exception, an FK cascade in the deleting transaction, so deleted text is never findable). H12 answered "show the seam": two labelled groups, `On this device` and `From the server`, never merged, so a late server answer can only ever append — the palette takes filtering and ordering off `cmdk` and anchors its cursor to a row identity. H9: text config `simple`, not `english`, via `SEARCH_TEXT_CONFIG`, with the job rebuilding the one expression index when it changes. H11: `needs_triage` and `canceled` issues are returned, visibly labelled. One entry point: Cmd-K's "Jump to issue" group is replaced, Enter on `Search everything →` opens a full `/search?q=` route. The route refuses to be a **permission oracle** — miss, out-of-scope, blank/short/unparseable query and statement timeout all return byte-identical `200`s. **Queries are never logged**, asserted by a test, and `search_document` is never an AI data source | ✅ built & archived (PR #12, #14) |
| 14 | `auto-status` | **The last inch of the wedge: PR state actually drives issue status** — opt-in **per team**, off by default, so an instance that adopts nothing is byte-identical to before. Two transitions and only two (linked PR `open` → In Review, `merged` → Done), justified against the fixed status set; `draft` and closed-unmerged are declined rather than deferred. Forward-only along Backlog → Todo → In Progress → In Review → Done, never onto or over `canceled`, never onto an untriaged issue. One nullable column carries both the switch and its **epoch** (`team.auto_status_since`): an event older than the instant you enabled it can never drive status, so first-install backfill and the reconcile sweep cannot retroactively rewrite a mature board. A human's status write wins whenever it is newer than the **event's own** timestamp — a comparison, not a grace window (`issue.last_human_status_at`, backfilled from `updated_at`, stamped by every status-writing mutator **except** the system principal, which is the machine-readable record that a write was not a person's). Fires on a state **edge**, never a state, so redelivery, ETag reconciliation and pg-boss retries are idempotent for free. The write goes through the shared `issue.setStatus` mutator under the **system principal** promoted out of `apps/server/src/jobs/cycles.ts` into `packages/schema` — never attributed to the PR's author — and the whole decision lives behind the `WorkGraphMutation` union, so `apps/server/src/connectors/github/` is not touched at all and a GitLab connector inherits automation with no feature-code change. **Divergence is unchanged and is the fallback by construction**: quiet when a transition fires (nothing left to disagree about), firing when automation is off *or blocked*. Migration **`0016_auto_status`**, an admin-only *Status automation* section on `/settings/connectors`, and **no new container, job, env var, or GitHub App scope** | ✅ built & archived (PR #15) |
| 15 | `attachments` | **Provider-neutral storage seam** (`StorageProvider`, shaped like `Mailer` — `put`/`get`/`delete`/`health` and deliberately **no `getUrl()`**) with a local-filesystem implementation that is the complete default and optional S3 via `aws4fetch` — no vendor SDK, no fourth container, `S3_ENDPOINT` covering R2/B2/Garage/an existing MinIO. Upload, serve, attach-once and idempotent-delete routes under `/api/v1/files`, migration **`0017_attachments`** (one table, `team_id` as the permission anchor and `teamScoped` join target, `ON DELETE SET NULL` on both edges so a deleted comment *orphans* its files instead of cascading), `sharp` thumbnails at upload — the first native module in the runtime image — and an orphan GC sweep as a fourth block on the **existing** pg-boss scheduler. Content type is **sniffed from the bytes against a five-format raster allowlist**, never taken from the upload's claim, so an SVG is served as `application/octet-stream` + `Content-Disposition: attachment` and never rendered in-origin. **No signed URLs, ever**: an `<img src>` in a Zero-synced document replicates to every client's IndexedDB, so the stored node carries an opaque id and the app proxies bytes for both providers — which is also what makes the permission check the same code either way, guarded by a CI grep that fails on a presign-shaped name or a seam member beyond the four. The read path refuses like `search`: a member of another team asking for a real id gets a response **byte-identical** to one for an id that never existed. **The rows sync but there is no `attachment` mutator anywhere** — a row without bytes is meaningless and a Zero mutator cannot carry one, so a client cannot forge a row and the derived agent-tool registry gains nothing it could call | ✅ built & archived (PR #16) |
| 16 | `editor-markdown` | Markdown as the **interchange** format, Linear's model: TipTap JSON stays the storage — no migration, no Zero schema change, no jsonb column touched. `@tiptap/markdown` at the exact 3.28.0 pin (the catalog's TipTap block grows from five entries to six), the two missing input rules (`# ` heading, `[text](url)` link), paste-markdown-in with four refusals, and copy-markdown-out on the plain-text clipboard flavour only — the rich `data-pm-slice` flavour is untouched, so an in-app copy/paste stays lossless. **The serialiser is corrected, not used as shipped**: `@tiptap/markdown` optimises for round-tripping through *itself*, HTML-entity-encoding all non-code text (`a < b & c` → `a &lt; b &amp; c`) and escaping no block-leading characters (a paragraph reading `# not a heading` re-parses as a heading). One defect, not two — `>` survives today only *because* of the entity encoding — corrected by replacing one private method in `packages/ui/src/lib/markdown.ts`, which is in `packages/ui` and not `packages/schema` because `apps/server` imports schema. The parse also **refuses raw HTML outright** — `parse()` otherwise hands anything tag-shaped to `generateJSON` against the whole extension set, so `<div>hello</div>` lost its tags and a `data-type="mention"` span pasted as text minted a real mention. That doc-level rule is now **mechanical**: `scripts/check-boundaries.mjs` gained a third rule refusing any TipTap/React/ProseMirror/UI import under `packages/schema`, and the matcher (`scripts/lib/boundaries.mjs`) has fixture tests of its own. A mention exports as `@Display Name` — lossy but readable — and never comes back as a mention, so no paste can invent a notification. A down payment on the export gap: it builds and proves the serialiser, and ships none of the export surface | ✅ built & archived (PR #17) |
| 17 | `editor-rich-content` | Images, tables and syntax-highlighted code blocks in the editor, plus a `/` insert menu (a **sibling** of the mention controller with its own `PluginKey`, never a generalisation of it — the mention list carries eligibility state and "why not" copy a command list has no use for) and the issue **Files** section. Consumes 15's contract — the stored node carries an opaque `attachmentId` and **no URL, not even a relative path**, and the renderer computes `/api/v1/files/<id>` — and extends 16's serialiser rather than a second one: GFM pipe tables (with two corrections to `@tiptap/extension-table`'s shipped renderer — a prose `|` split the cell, and a multi-block cell emitted a literal U+001F), `![alt](path)` on the clipboard only, and fences that keep their language. **Its one irreversible decision is the schema-skew guard.** The ProseMirror schema is versioned by the deployed *bundle*, not the database, and TipTap drops unknown node types on parse — so a tab open across a deploy would autosave a pruned description over the real one and LWW would make it the truth, silently. yapm **refuses the write**: `sanitizeRichText` stamps a monotonic `RICH_TEXT_SCHEMA_VERSION`, a zero-import `detectRichTextSkew` walks the raw JSON against type sets *derived* from the extension set, and a blocked document renders read-only with a "reload to edit" banner — structurally, with no editor instance to type into. Built before the first new node type, because retrofitting it means the data is already gone. Six catalog entries (four TipTap at exactly 3.28.0, `lowlight`, and **`highlight.js` — BSD-3-Clause, the first non-MIT/Apache runtime dependency in the client bundle**), fifteen curated grammars rather than `lowlight.common`, and **no `highlight.js` stylesheet**: seven `--code-*` tokens in all six theme blocks, each asserted at AA. No migration, no Zero schema change, no new synced query, no new mutator, no diff in `apps/server` | ✅ built & archived (PR #18) |
| 18 | `retro-ai-draft` | **AI proposes, the team disposes — first half.** The AI reads the just-closed cycle's team-level work graph and drafts ≤3 Wins / ≤3 Losses / ≤3 Improvements into the retro change 10 already auto-opens, each citing a work-graph entity id or a **seed metric key** (so the UI renders yapm's own value and sparkline beside the sentence — the model can point at a number, never type one). `generateStructured` only: no tools, no `runAgent`, no HITL surface, no write path — a correction to the research, which designed this as an agent run. **Generation is lazy, at the `brainstorm → group` phase advance**, not at cycle close: the server-only branch of the shipped `setPhase` override writes a `pending` row and a self-re-arming pg-boss tail (the `SEARCH_INDEX_QUEUE` shape) sweeps it within seconds. That replaces both options the scoping pass offered — no staging table, no unverified phase-filtered ZQL — and it makes the anchoring mitigation structural for free (during `brainstorm` the row does not exist), while making spend proportional to use: a retro nobody runs costs nothing. New server read `retroFactsForCycle` (`buildRetroSeed` had only ever been called client-side), with an explicit column allowlist because `review` carries an `author` column. Migrations **`0018_retro_ai`** (`retro_ai_draft` + `retro_ai_proposal`, both `teamScoped` and **client-read-only** via the `upsertCycleDigest` trick) and **`0019_ai_retired_spend`** (`team.ai_retired_spend_usd`, so a draft deleted when a facilitator steps back to `brainstorm` does not take the money it cost out of the workspace spend total). Also owns the three shared refactors every later AI change binds to: generalize the cite-or-omit and name-validator walkers off `DigestContent`, union `getWorkspaceAiSpendUsd` across artifact tables, and share the `pending\|ready\|failed\|ai_off` status. AI off ⇒ the change-10 retro is byte-identical | ✅ built & archived (PR #20) |
| 19 | `retro-ratification` | **AI proposes, the team disposes — second half.** `retro_ai_reaction` synced **self-scoped with no admin bypass** (the `retroDrafts.mine` / `retroVotes.mine` shape), one new `react` op in `RETRO_WRITE_OPS` allowed in `group` and `vote` so the phase machine, stepper and CHECK constraint are untouched. **Tallies are never incrementally maintained**: the verdict and counts are computed once, server-side, inside the existing `setPhase` override on `vote → discuss`, which sidesteps both concurrency classes this repo already paid for (the lost-update class behind `bumpRetroVoteTally`'s single-statement upsert, and the per-actor lock class behind `lockRetroForVote`). Verdict rule fixed and knob-free: any single disagree ⇒ contested (a minority veto protects the quiet dissenter), unanimous agree ⇒ agreed, majority disagree ⇒ rejected; contested sorts to the top of discuss. An agreed Improvement is one keystroke to `retroAction.create` carrying `retro_action.ai_proposal_id`, from where the shipped idempotent server-numbered `convertActionToIssue` takes over unchanged — **and never pre-fills an assignee**, which would be the first per-person output in the entire AI layer. Migration `0020_retro_ratification` | ✅ built & archived (PR #21) |
| 20 | `pm-digest-boundary` | **The first change in yapm whose output crosses a permission boundary** — so it ships the boundary while the content is deliberately unremarkable. A separate synced `pm_digest` row, **not** a widened read on `cycle_digest`, forced by ZQL having no `select()` (a PM-audience query over the existing table would hand the PM the team-internal `content` column). A new read predicate function, **never** a change to `teamScoped`, which ~15 queries depend on. Two default-off switches, a per-team audience map and an admin kill switch, all in the existing admin-gated `connector_config.config` jsonb — no new table, no new crypto — plus a server-only `ai_disclosure_audit` table excluded from the Zero schema, which ships *here* because "auditable vs ambient" is the strongest line in the security story and there is no audit table anywhere in migrations 0001–0017. Content is a second `generateStructured` run over the **existing** `cycleFactsForTeam` under a PM altitude prompt, with evidence baked as server-rendered plain-text labels rather than links (a PM outside the team can open none of the targets). **Its one spec delta**: `ai-agent`'s "AI-off renders raw evidence" SHALL is unsatisfiable for a reader with no raw evidence — for the PM digest, degrading gracefully means the surface is cleanly absent. Migration **`0021_pm_digest`**. Honest about what it is: on its own the model would see only issue titles and PR titles, because migration 0009 stores no PR body, labels, commits, paths or diffs — which is why **change 21 was reordered ahead of it** and now supplies the product-area substance a PM-facing digest would otherwise lack | ✅ built & archived (PR #22) |
| 21 | `pm-digest-areas` | **The actual PM differentiator, and it needs no patch content.** Extend the hand-written narrow `GithubRestClient` with `pulls.listFiles`, fetched transiently in the job under the **already-granted** Pull requests + Contents read permissions — no new App scope, no re-consent, nothing persisted (and the projection must drop the `patch` field the API returns unasked, proven by a mock that includes one). An admin-editable path→area map converts file paths into yapm-computed area labels **before the model runs**, so raw paths never enter its context at all — the same structural move as removing the identity dimension, and far stronger than a post-filter. Adds area grouping, change-size bands, touched-sensitive-area risk flags and the "N internal improvements" collapse; extends `buildCycleFacts` with area/size fields, still identity-free, still yapm-computed. Also lands the runtime **disclosure validator** — a deterministic sibling of `dropItemsNamingMembers` dropping any item bearing a path token, a source-file extension, a fence or an `identifier.method()` shape — here rather than in 20, because this is where path-shaped strings first exist. No migration (config jsonb). **Ships before change 20**: the family was reordered so the substance lands on the existing team-internal digest first, and the second authorization axis is then judged with something real to disclose | ✅ built & archived (PR #19) |
| 22 | `retro-ai-loop-close` | **"Did last cycle's improvements ship?" — the fact that makes the retro compound.** Extends `retroFactsForCycle` with the prior retro's actions and their converted issues' live statuses, **stripped of `assignee_id`** (both `retro_action` and `issue` carry one, making this the only place the fact assembly touches an identity-bearing column, so it gets its own stripping test asserted against the built object as well as against the SQL). The allowlist grows by exactly `retro` and `retro_action` and the equality assertion is what makes that "exactly": an action is the team's agreed public output, made in the open, carrying no author column, while a card is one person's testimony — and `retro_action.card_id`/`.group_id`, the join back to the anonymous card, are never selected. Outcome vocabulary is yapm-computed and knob-free (`shipped` is `done` **and nothing else**; `canceled` is its own outcome, never folded into "not shipped"; `not_converted` is kept apart from `in_flight`). **`follow_up` is a derived bucket over a new `retro_action` reference kind, not a fourth stored category** — `refs` is jsonb with no CHECK and `retro_ai_proposal.category` is a text column under one, so the derived bucket is what keeps the promise of no migration; one `retroProposalBucket` function drives the cap, the rank, change 19's comparator and the panel, and "no prior actions ⇒ no follow-up" falls out of change 18's shipped cite-or-omit validator rather than out of a first-retro branch. A prior-action reference's label, outcome and origin cycle are **baked by the server after validation** — the client cannot resolve that row from its own sync scope, so the chip is plain, non-navigating, and carries no string a model wrote. Also ships the optional half now that change 19's verdicts exist: the **rejected-proposal log**, one admin-gated `GET` behind the shipped `requireAdmin` and a read-only settings section, reading `retro_ai_proposal`/`retro`/`cycle`/`team` and **never `retro_ai_reaction`** — team-level with no per-person column to omit, and structurally out of the model's context, because a draft steering away from previously-rejected phrasing would be a model optimising for approval. Honest about the payoff curve: it reports nothing until a team has run two retros, and the first-retro surface is a clean absence. No migration. **Superseded in part by change 24**: the derived-bucket decision above (its design §D3) was reversed by maintainer choice — `follow_up` is now a stored fourth category under migration `0022`, and `retroProposalBucket` no longer exists. Everything else in this row still stands | ✅ built & archived (PR #23) |
| 23 | `pm-digest-governance` | **The promises that make "governed" true rather than rhetorical.** Retention sweep for disclosures as another block on the existing shared pg-boss (copying `NOTIFICATION_RETENTION_QUEUE`), an admin audit view over `ai_disclosure_audit`, and an optional "your cycle digest is ready" email carrying **a link only, never the digest body** — a mailed artifact sits outside the kill switch, outside retention and outside audit simultaneously, so link-only is a stated decision rather than an accident. Docs: a features page, a self-hosting disclosure-model page, ROADMAP/VISION/`.env.example`. **Must merge before any doc or marketing copy uses the words "auditable" or "retention-bounded"**. No migration | ✅ built & archived (PR #24) |
| 24 | `retro-followup-category` | **A logged decision reversed, on the spec the decision itself left behind.** Change 22 shipped `follow_up` as a bucket *derived at read time* — a proposal was a follow-up exactly when it cited a `retro_action` reference — because that change was scoped with **no migration** and a sibling build held the next free number. Its design §D3 recorded the stored enum as *semantically cleaner and the smaller diff*, gave the migration SQL, and closed with "if a maintainer prefers the explicit enum, the work is one file and this paragraph is the spec for it." The maintainer chose the enum, so this change is that paragraph executed: `RETRO_PROPOSAL_CATEGORIES` gains a fourth value, migration **`0022_retro_followup_category`** swaps the CHECK (asserted against live Postgres by the drift test), and **`retroProposalBucket`, `RETRO_PROPOSAL_BUCKETS` and `BucketableProposal` are deleted along with every call site** — the cap, the rank, change 19's comparator, the panel's grouping and the category chip each collapse to reading `proposal.category`, because all five already keyed off the constant. The one thing a stored category does not get for free is the property the derivation gave away nothing for: with a derived bucket "no prior actions ⇒ no follow-up" fell out of change 18's cite-or-omit walker, and with a stored one the model can emit `follow_up` citing nothing but a valid issue — so `dropUnbackedFollowUps` restores the implication explicitly, positioned **after** the caption bake (which can orphan a follow-up) and **before** the cap (which must be last). The ≤3 cap applies to follow-ups on the same line of code as every other category. **No backfill**: a pre-change row storing `improvement` and citing an action now renders as an Improvement, because nothing stored recovers whether the model meant a report or a repeat, and a migration that rewrites model-derived content is not a migration. `RETRO_SEED_REF_KINDS` keeps `retro_action` — this removes the *derivation*, not the reference kind that §D4's baked label depends on. No new dependency, no new container, no new query, no new mutator | ✅ built & archived (PR #25) |
| 25 | `deployment-hardening` | **The last mile: every feature was built and archived while the product could not be correctly self-hosted.** Four compounding defects, each verified against source. The README's quickstart passed no `--env-file`, so `-f docker/…` made `docker/` the project directory, no `.env` was ever read, and a production stack silently ran on the secrets this repository publishes — `BETTER_AUTH_SECRET` among them, which encrypts the JWKS private key at rest, so a known value plus any database read mints a sync token for any user. Fixed in three layers: a generator (`scripts/init-env.mjs`, dependency-free so it runs before `pnpm install`), a **boot refusal** that exits before migrations naming every variable still at a shipped default (one escape hatch, `YAPM_ALLOW_INSECURE_DEFAULTS`, for evaluation boxes), and a non-gating `/readyz` entry for the operator who missed the log. The published image could never sync on a real host: the browser's zero-cache origin was a Vite **compile-time** constant baked to `http://localhost:4848`, with no runtime override anywhere — now `GET /api/config` from a validated `ZERO_CACHE_PUBLIC_URL`, fetched **above** the Zero client so none is constructed on a placeholder. No image had ever been published, because the publish job was gated behind a release job that fails on a repo setting no agent can flip — decoupled, so `edge` and `sha-<7>` go out on every push to `main`. And the documented upgrade command did not work, while rollback after a forward-only migration was an undocumented crash loop. Three new self-hosting pages (deploy & harden, upgrade & rollback, and a configuration reference bound to the Zod schema by the same set-equality test that guards `.env.example`), and SECURITY.md corrected to claim no signing, no provenance and no scanning — because this change makes none of them true. No migration, no fourth container | ✅ built & archived (PR #26) |
| 26 | `sso-admin-gating` | **A security fix and an honesty fix, which had to ship together.** better-auth's SSO plugin was registered as bare `sso()` and its whole handler mounted at `/api/auth/*`, so `POST /sso/register` was gated by a session and nothing else — on a public instance **anyone who could sign up, including an authenticated non-member sitting at the AccessGate, could bind their own IdP to any email domain**, after which yapm itself would redirect that domain's real employees, from yapm's own origin, to an attacker-controlled authorization endpoint. Every other configuration surface in the product was already admin-gated; this one was not. The seven provider-**management** paths are removed from better-auth's router with `disabledPaths` — an **exact** match on the normalized pathname, which is why no callback can be caught by it — and the only door becomes `/api/v1/sso`, built from the `requireAdmin` shape `/api/v1/connectors` and `/api/v1/ai` already use, evaluated **before** `:providerId` is read so a member asking about a provider that exists and one that does not get the identical 403. The eight sign-in and callback paths stay anonymous, asserted in the **same test file** as the refusals so a future tightening cannot silently take sign-in with it. `sso()` gains `domainVerification` (a DNS TXT record resolved in-process with `node:dns/promises` — no fourth container) and a `providersLimit` that must never be `0`, which the plugin reads as *registration disabled*. The honesty half: `/api/auth-methods` reported `sso: true` **unconditionally** and `useAuthMethods` defaulted it true, so **every** instance showed a Continue-with-SSO button that led nowhere — yapm shipped no supported way to register a provider at all. Availability now means *at least one registered provider whose domain is verified*, the client default flips to `false` to match `github`, and the login form's own code needed no change: it already collapsed the divider when both providers were absent. A provider is treated as **workspace configuration, not the registering admin's property** — the plugin scopes rows by `userId`, which would otherwise strand a workspace when that admin leaves — so a write transfers the row's ownership pointer first, the one field of a better-auth-owned table yapm writes. New `/settings/sso` beside `/settings/ai` and `/settings/connectors`: register, publish the TXT record, verify, delete; client secret write-only; verified state carried by **words** in a neutral badge, no new colour pair. **No migration** — `getMigrations()` owns `ssoProvider` and the drift test learned its real DDL from live Postgres, which is how three assumptions died (no `createdAt`/`updatedAt`, `domainVerified` **nullable with no default**, `oidcConfig` `text` not `jsonb`). Free means free: admin-gating **who configures** SSO is not a gate on **who uses** it, and the delta spec says so in a scenario | ✅ built & archived (PR #27) |
| 27 | `deploy-history-edge` | **The last edge in `issue ↔ PR ↔ CI ↔ deploy`, and the fact that had been arriving and being destroyed.** `deployment` carried `state` and `updated_at` and no immutable event timestamp, `applyWorkGraphMutation` updated state in place, and the reconcile sweep re-derived it from the **newest status only** — so GitHub's `auto_inactive`, which flips a superseded deployment to `inactive` the moment the next one succeeds, overwrote every past success *and the moment it happened*. What the table held was "the current deploy per environment", and counting `state = 'success'` counted roughly one row per environment forever. Migration **`0023_deploy_history`** adds `deployment.deployed_at` (write-once), `deployment.sha` — which `map.ts` had been **dropping** since change 8 while the PR mapper two hundred lines above it correctly kept `head_sha` — and `pull_request.merge_commit_sha`, all nullable, plus `(team_id, deployed_at)`. The invariant lives in the shared write path, not in a trigger: `inactive` never stamps (its timestamp is the moment of supersession), the update path writes `existing.deployedAt ?? incoming` (write-once, never `min`), and the **stale-event branch stops returning blind** — a redelivered older `success` arriving after a newer `inactive` stamps the fact and *nothing else*, because the sweep only ever sees `statuses.data[0]` and nothing else would ever recover it. The second half fills a **reserved slot**: `merged-not-deployed` was selectable, spec-sanctioned and unconditionally empty, and it now evaluates an **exact, same-repo merge-commit join** (`pull_request.merge_commit_sha = deployment.sha`) computed client-side over rows both surfaces already sync — chosen over the repo+environment+first-success-after-merge heuristic, which would let the strip assert "this reached production" about a revert or a hotfix that never contained the change. The exact join **over-reports** for a batched or retagged deploy and can never under-report, and no `head_sha` fallback exists, because a deployed branch is not a deployed merge. `DeliverySignal` gains a fourth axis, both seam function signatures unchanged; the row gains a deploy glyph on `--signal-sync` labelled "Deployed" with **no environment claimed** (yapm cannot know which of a team's environment strings means production — an admin-set answer belongs with the view that will care). **No backfill migration**: a timestamp that was overwritten cannot be invented, so the existing reconcile cron is the backfill and a sparse first week is documented rather than fixed. No delivery view, no metric tile, no incident entity (change failure rate and MTTR need one and are honestly Phase 3), no new container, dependency, query, or App permission | ✅ built & archived (PR #28) |
| 28 | `team-delivery-view` | **Half the README's headline promise was already true and completely invisible.** "DORA and code-review health are just views, not a $30/developer/month add-on" — and the five flow metrics behind it were computed, tested, and reachable at exactly one URL: inside a single retro, scoped to one cycle. This gives them a home. **No formula is duplicated**: `deliveredCounts`, `flowMeasures`, the medians and the carryover tests move out of `retro/seed.ts` into `packages/schema/src/zero/metrics/scope.ts` and are generalized from *one cycle* to a `DeliveryScope` of many — every membership test that compared against `cycle.id` becomes a `cycleStarts.has` lookup, which for a one-entry map reduces to the expression it replaced, so `retro/seed.test.ts` passes with **not one character changed** (that, plus a golden snapshot of `buildRetroSeed` committed *before* the refactor, are the two tripwires the whole change is guarded by). `metrics/window.ts` adds the rolling reading: value = the whole window evaluated once, sparkline = one point per cycle, delta = the window immediately before, dropped entirely until two full windows exist. The presentation lifts out of `apps/web/src/retro/` into `apps/web/src/delivery/` with the retro's `data-testid`s passed in as props, so the shipped e2e selectors and the retro's own tests are untouched. The window is **completed cycles, not days** — four Delivered metrics are defined relative to a cycle boundary — bounded at **12**, which is the answer to "no server": Zero has no aggregates, the view adds **zero query surface** (`cycles.byTeam` and `issues.byTeam` are already synced by the issue list *and* the retro), and changing the window re-runs a pure function over rows in memory. Blamelessness is re-proven at the NEW entry point rather than re-argued: one shared walker behind a new `@yapm/schema/testing` subpath, run over the built object from input rows that are supersets carrying `assignee: {name, email}`, `creator` and `review.author = 'octocat'` — plus a string check on the serialized model, so a caption that interpolated a login fails even though the shape check would pass. **Team-level only** at every depth: no filter, no drill-down, no tooltip that names anybody. Honest about what it is not: a permanent, non-dismissible block on the page names deployment frequency (change 27 now stores the durable deploy history it needs; the metric over it is a later change), change failure rate and MTTR (both waiting on an incident entity that does not exist) as absent, and lead time as partial — open→merge only. **No migration** | ✅ built & archived (PR #29) |
| 29 | `team-home-digest` | **The team's own page was the last thin surface in the product** — a members roster at the exact URL a teammate opens first every morning, while every fact a morning actually needs was already on their device: cycle facts, the divergence flag, PR/CI/review state, the durable deploy history, triage, notifications and the stored cycle digest. This change makes `/teams/{teamId}` the **morning digest**: hero with day band, committed / landed / added scope (the `metrics/scope.ts` semantics, carry-ins included) and a narrative that is the stored `cycle_digest` when ready content exists and a deterministic ≤2-sentence fallback over real counts otherwise — never filler, never a model call at page-open; **one attention number** over four *disjoint* exception classes assigned by precedence (`status_behind_merge` → checks failing → review waiting > 24h → triage), a distinct-issue count by construction that renders identically everywhere it appears; a literal trailing-24h SINCE YESTERDAY (no invented "you left Tue 6:40p" — the last-seen anchor is a deferred entity, so the page does not claim it); YOURS = the signed-in user's own in-flight rows ending "your work only — never compared", with the "No reviews owed" reciprocal rendered **only** under the team-level zero-open-reviews predicate because no reviewer↔user mapping exists to verify anything narrower; the Runway lane of predicate-phrased ready starts (Crit/Verify lanes fold — their entities don't exist); a weekly ship-cadence chart over the write-once `deployedAt` fact change 27 stores; Live / Built-not-live badges from the same exact merge-commit join as the reality strip; and a composed mono footline that names **only the folding rules the render actually executed**. Adaptive folding is the feature: every empty band is absent, not apologetic, and the page stays honest when all of them fold. **Zero new sync surface** — no table, no named query, no mutator, no permission change (the registry is byte-unchanged); every derivation is a pure function in `packages/schema` over rows already synced, so the page renders with no network and works offline. Members management moved intact to `/teams/{teamId}/members` behind a Members doorway; the e2e navigation contract (team page → "Issues", → "Board") is preserved in the onward footer. Blamelessness re-proven at the new entry point with the shared identity-key walker; four daylight tokens added to **every** theme block (warm-light literal, others derived via `color-mix` from their own palette, so no theme can miss them). No migration, no container | ✅ built & archived (PR #31) |
| 30 | `one-reality-vocabulary` | **The app drew delivery reality two incompatible ways.** An 86px strip of lucide icons (five PR-state git glyphs, a check/x/loader for CI, a rocket for deployed, a warning triangle for divergence) on the issue row and inside the issue detail's Delivery field; a 118px four-station track of nodes, segments and a `//` break on the team home — overlapping facts, no shared type, and the CI glyph defined twice with identical semantics. Every northstar mock draws reality as nodes and segments, so **the track wins** and the icon strip, the git glyph set and the triangle are deleted rather than deprecated. The track is generalized so the three page rebuilds after this compose it instead of forking it: exported node/segment kinds, an exported `buildRealityShape(strip, {divergence})` where the `//` is a **segment kind** whose position is derived from which divergence fired rather than a hardcoded broken shape, composable orientation (the dense horizontal row, the same shape at any measure, and the vertical rail with a sentence and a mono fact per station), and one `DeliveryStrip` type replacing the incompatible `RealityStripProps` / `TeamHomeStrip` split. Status arcs and priority ticks are reconciled to the mocks' own geometry — status as **cycle position** (dashed ring → open ring → half arc → three-quarter arc → filled disc), priority as **weight** in ticks with urgent as one tick standing alone — rather than adding a third set. `DayBand`, `ScopeBand`, `TickBar`, `TriageDots` and `CadenceChart` are promoted out of `apps/web/src/home/` into `packages/ui` with `drawn.tsx` and `cadence-chart.tsx` deleted outright, no shim and no duplicate. Three shared patterns land as vocabulary with no consumer yet, because the pages that mount them are the next three changes: **the peek** (hover *or* focus opens, ⏎ goes, esc stays, and at most one open per page **by construction** — the provider holds one id, so a second open peek is not a bug that can happen), **the how** (a derived number carries a quiet mono `how ·` and explains itself only when asked; click or Enter, never hover), and **the provenance mark** (monochrome, 12–14px, `currentColor`, after the fact it sourced — with no `color` prop, no numeric size and no `upload` member, so the rules are enforced by the type rather than by a comment). Contrast is part of done: every fact-carrying node at 3:1 and the `//` break and mono fact lines at 4.5:1, against the plain, hovered, selected and divergence-row surfaces in all six theme blocks — which found two real failures (focused light's in-review hue at 2.88 over the selected row, warm dark's urgent ink at 4.35) and fixed both in the tokens. **No new table, no named query, no mutator, no permission surface, no migration** — the query registry is byte-unchanged; this is presentation over facts that already sync | ✅ built & archived (PR #32) |
| 31 | `app-frame` | **Every page had its own chrome, and the doorways fell out of it.** `AppShell` was one sticky header used by ten routes; the nine team work surfaces each hand-rolled a copy of it and silently dropped search, digests and the inbox, so the three doorways were invisible on exactly the surfaces being overhauled — while an eight-item pill nav (`ViewSwitch`) was the de-facto navigation and `⌘K` was bound by four independent `window` listeners. This change builds the northstar's **three-band frame** (`ia.html`): band 1 the **deck** (48px, identical everywhere — workspace · team · six stops · ⌘K pill · attention badge · Inbox · you, with `more▾` a transient carrying Retros/Projects/Roadmap and their `g`-shortcuts, Decisions folded away for want of an entity), band 2 the page-owned **masthead** (title · count · lens · meta · actions) that every hand-rolled header is migrated onto, and band 3 the **statusline** (32px — the team's day, and the sync state that subsumes the old `ConnectionStatus` pill). **One attention number**: the deck badge, the statusline segment and Home's NEEDS ATTENTION are one `buildTeamFrame` derivation that `buildTeamHome` consumes, so they cannot drift; at zero they are absent, not zeroed. Board becomes a **lens** in the Issues masthead rather than a peer destination, and `ViewSwitch` is deleted. `⌘K` gets one global owner that per-surface palettes register with. Off-team the frame degrades honestly — the stops point at an anchor team, the statusline asserts nothing about a team it is not on. **No new table, no named query, no mutator, no migration** — chrome over facts that already sync | ✅ built & archived (PR #33) |
| 32 | `issue-list-daylight` | **The list drew its reality and then refused to say it.** PR #32 gave the row its drawn vocabulary and PR #33 gave the page its frame; what was left between them was the work surface, where the shipped list still shouted (seven outlined `lucide` buttons and two native selects) and stayed silent (a 118px drawing and an `aria-label` nobody reads carrying the whole reason the list exists). This change rebuilds the list to `northstar/issues.html`. The row gains a **phrase at rest** in a reserved slot — `Checks failing`, `Done in git, not on the board`, `Built — not live yet`, `In review — waiting 16h` — and a quiet row stays **genuinely blank**, because the slot is reserved whether or not it is filled and a signal populating must not shove a neighbour's track sideways. Those strings were already in the product, **module-private** inside `team-home.ts`; they are extracted into ONE shared dictionary beside the delivery seam, keyed by a classifier over the real predicates only (`computeDeliverySignal` + `computeDivergence`) and rendered in two **registers** — `neutral` for the list, `personal` for Home's YOURS — both total over the key set, either of which may resolve a key to *silence*. Home's rendered strings are byte-unchanged and `team-home.test.ts` is untouched, which is what proves it. A **source-level test** fails if the dictionary's strings appear in a second production module — it found one on the first run. Provenance rides on the **dictionary entry**, not the caller: the GitHub mark suffixes exactly the check facts and the deploy fact, never the divergence or a review age, so two surfaces cannot disagree about where a fact came from. The review phrasing splits on `reviewAgeFrom` — `waiting` only when nobody has reviewed, `reviewed N ago` when one came back — because there is no review-requested event and the shipped honesty rule forbids claiming a reviewer waited. The filter bar is **re-registered, not rebuilt**: a filter mark then plain-text axes, with `Group X · Sort Y` quiet at the trailing edge, and every capability intact — seven axes (accessible names preserved verbatim; four e2e specs drive them), the search field the mock does not draw but a cut would be a regression, seven groupings (`Group by` stays a native `<select>` because a passing keyboard assertion drives it), six sort keys and the direction pair folded into one menu, and saved views with their cycle/project downgrade. Group headers take the mock's tinted band, the grouping's own mark and a mono **post-filter** count. The **fold** states the real remainder (`↓ 109 more` = a subtraction over the true filtered length, never a constant), is a real focusable button `j` reaches from the last row, and opens with focus on the first revealed row — while the full filtered set still drives the masthead count, the selection targets and the palette context. Three sentences become labels and the page carries **zero** explanatory prose. Contrast in all six blocks found one real failure: the mock inks the selected row's mono key with the accent, and `--accent-strong` measures 3.84–4.38 on `--bg-selected` in two presets — so the ink steps up to `--text-1`, the rail and tint carry the state, and the measurement is recorded rather than deleted. **No new table, no named query, no mutator, no migration** — every phrase derives from rows the list already syncs, through the same one-pass deployment index | ✅ built & archived (PR #34) |
| 33 | `issue-detail-bridge` | **The page named after yapm's whole argument had none of it.** PR #32 shipped the vertical rail with **no product consumer**, PR #33 shipped band 2, PR #34 shipped the phrase dictionary — and the issue detail read none of them: no masthead, no phrase, no rail, a `Delivery` property that drew the same 118px track the row already draws, and a `$issueKey` route that parsed digits out of the segment and **linear-scanned the whole team's backlog** to find one row. This change rebuilds the detail to `northstar/issue.html`. Band 2 gains a **kicker** (breadcrumb · mono key · the divergence pill in the dictionary's own words, never colour alone) and, in `meta`, the **two-register subline** the page is named for: a plain line for a PM (`◐ In Progress · Cycle 2 · ● feature · Done in git, not on the board`) directly above a mono line for an engineer (`git merged 8f21c4a · PR #188 · drifted 22h` + GitHub's mark), both built from ONE `computeDeliverySignal` and ONE timeline so the registers cannot drift. The right column mounts the shipped **vertical rail** — one station per moment a durable timestamp supports, header stating only the chain it drew (`idea → built → live`) — followed by the **divergence callout** whose evidence contrasts `lastHumanStatusAt` with the merge and whose two actions are real: `Mark Done ⏎` through the existing mutator, `Keep as is esc` dismissing locally with **no write**, so the pill, the `//` and the phrase all persist. ⏎/esc are bound inside the callout's focus scope; ⌘K stays the frame's. One new derivation, `buildIssueTimeline` in `packages/schema`, feeds the rail AND the feed off one clock, so a moment cannot be dated two ways on one page. **Four blocks fold away because nothing backs them**: the designed station (no design entity), status history (`lastHumanStatusAt` is one scalar, so the mock's `board todo → in-progress` is two claims it cannot make), backlinks (no issue↔issue table, no mention edge, no path to a retro action) and PR comment counts. `IssueDetailBody` takes `layout: 'page' \| 'sheet'` so `?open=` and the full page are one implementation of every section; the `Delivery` property is removed because the rail states it at full measure. One new named query, `issues.byKey({teamId, number})` — the ONE case the scoping decision reserved — carrying its siblings' `teamScoped` predicate character for character plus `needsTriage`, since a key is guessable and an id is not. **No new table, no new mutator, no migration** | ✅ built & archived (PR #35) |
| 34 | `delivery-journalism` | **The last northstar surface, and the one whose subject is honesty.** PR #32 shipped the peek and the `how ·` as vocabulary, PR #33 the frame, PR #34 the phrase dictionary — and the Delivery page consumed none of them: four bordered metric cards, two paragraphs of explanatory prose and a bulleted `What this doesn't show yet` panel. This change rebuilds it to `northstar/delivery.html` as a **journalism cut** — each section leads with a sentence stating what the data says, then draws the evidence. The whole page is ONE model, `buildDeliveryPage` in `packages/schema`, so what the page states is testable as data rather than as rendered HTML: the standfirst (the cycle in progress · the window · the binding rule, which appears **exactly once in the product**), an **annotated timeline** over the active cycle whose every annotation is derived (deployments at their own moments, the retro mark from `closedAt`/`title`, `day N of M`, and the call-out chosen by a stated rule — the first successful deployment of the ISO week carrying the most, ties to earliest), four **stat readings** each with its delta in words and its own `how ·`, a **distribution strip** where one dot is one merged pull request and the median is drawn WHERE IT FALLS from the same field the tile states, a **flow band** with carryover ribbons and `+N added` caps, and **review-rhythm** small multiples that carry no reviewer at any depth. Two things the mock says are refused: the retro's agreed action (it lives on a row carrying an assignee) and *the dots have been denser since* (a causal claim yapm does not get to make — the subline states the two counts and the date with no verb joining them). **The population was wrong and is fixed**: `pullRequests(scope)` flat-mapped the issue subtree, so a change linked to two in-scope issues counted twice in every median and rate — invisible in a tile, glaring as two dots for one change. **The honesty line is corrected, not ported**: the mock said merged-to-live isn't measured, and it now IS (the exact merge-commit join of change 27), so repeating it would ship a NEW lie on the page whose subject is not guessing. The line names change failure rate, time to restore and **deployment frequency as a rate**, and `more ·` discloses the coverage limit nobody had stated: a pull request reaches this page only through an issue. Four drawn components land in `packages/ui` with structural props, no schema import and no literal colour; contrast in all six blocks moved the median label and the today label off `--accent-strong`, which measures 4.44 on `--bg` in editorial light. **No new table, no named query, no mutator, no migration** | ✅ built & archived (PR #36) |
| 35 | `design-corrections` | **What looking found.** The six-change northstar overhaul was rendered and read for the first time, and four things were wrong. **(1) Quiet rows draw ink they have no fact for.** An issue with no linked change drew a dotted placeholder track on every dense row — four hollow nodes joined by dotted segments — and `issues.png` draws NOTHING there: the mock reserves the slot and lays down no ink until there is a fact. On a real list that is sixty of sixty-nine rows repeating one ornament. The rule is now ONE exported predicate over the shape (`isQuietTrack`: every station empty and no break), applied inside `HorizontalTrack`, so the issue row, the team home's YOURS and attention rows and the board card all get it without a call site opting in — and a second empty state cannot start. The slot keeps its measure and its age column, proven by a width-parity test that gained the age comparison rather than losing anything, and it is `aria-hidden` with no label, because announcing "No delivery signal yet" on most rows of a list is the audible form of the same ornament. A track carrying ANY fact still draws its empty stations and dotted segments: the scaffolding joins facts rather than standing in for their absence. The **vertical rail is the deliberate exception** — the issue detail's subject IS the change, so a blank rail there would be less honest, not more. **(2) `done` lost its check.** PR #32 reconciled the status glyph to the mock's geometry, where status is cycle position and `done` is a plain filled disc, and dropped the check the product's own glyph carried. It is back as a knockout in `--bg` on the shared 20-unit grid at the shared 1.6 stroke with round caps — one family with the arcs — and the knockout is asserted at 3:1 against BOTH hues the glyph is ever inked with, because a check that vanishes on the digest's urgent row is a `done` that reads as a plain disc exactly where something went wrong. **(3) The statusline said `Connected`.** Every mock draws band 3's right end as `● Synced`; the shipped label was inherited verbatim from the old header pill. One string in `summarizeConnection` moves. No other state's wording moves — connecting, offline, an expired sign-in, a sync error and closed each say what is true — and `data-testid`, `data-connection`, `data-recovery` and the retry control are byte-identical, which is why fifteen e2e specs read the STATE name and none had to change. **(4) The in-progress amber failed contrast, and the mock is wrong.** `--status-in-progress` measured **2.69 / 2.17 / 2.87** against `--bg` in the three light presets — under the 3:1 non-text bar (WCAG 1.4.11) — while also inking text-sized elements; `delivery-journalism` measured it and deferred it as a cross-surface decision. The lights are retuned on the **held OKLCH hue** to 3.54–3.62, at 3:1 on all **seven** grounds a row or card is painted on rather than on `--bg` alone (on the selected row the old value sat at ~2.65 — the exact failure focused light's `--status-in-review` was darkened for in PR #32); the darks measured 8.00–9.49 and did not move. The token **splits** rather than being dragged to 4.5 — one amber at 4.5:1 on near-white is a brown that closes on editorial's orange urgent — so `--status-in-progress-ink` joins all six blocks exactly as `--status-urgent-ink` does, and `drawn.tsx` §ScopeBand's 9px `+` is its one consumer. `contrast.test.ts`'s `>= 2.1` assertion, which existed to record the failure, is **replaced** by the real bars plus a hue-separation bound, because contrast is the wrong instrument for separation: amber against green never reaches 3:1 at any usable lightness, which is why the flow band's added cap is an outline. The mocks carry the failing amber, so `NORTHSTAR.md` gains a third divergence entry with the before/after numbers. **No new table, no query, no mutator, no permission surface, no migration — this is drawing and tokens** | ✅ built & archived (PR #38) |
| 36 | `triage-daylight` | **A deck destination inside the new frame, with a pre-overhaul interior.** PR #33 gave Triage the three-band frame, PR #32 (corrected by #38) gave the row its drawn vocabulary and PR #34 the phrase dictionary — and the page consumed none of them: a masthead reading `Engineering · Triage` (repeating a word the deck already carries two stops left), three outlined `lucide` `Check`/`Route`/`X` buttons bolted onto the outside of every row, and an empty state that was a paragraph explaining triage to itself. This change rebuilds it to `destinations/triage.html`. Band 2 states `Triage`, a mono count and `oldest first` — the count being the **same `triage.inbox` length** the deck badge, the statusline and Team Home read, so the one attention number keeps one derivation. The queue stays a list with **no fold**, because a queue you want to empty must show its own floor, and every row is `IssueRow` **unwrapped**, so a triage row and a list row line up column for column: reality slot **reserved and silent** (a triage issue has no linked change), phrase slot empty, age from **`created_at`**, and the trailing avatar the **reporter** — announced as such through one new optional `IssueRow` prop that overrides only the announced name, never the drawing. The row under decision **unfolds in place** into the issue's own description, a mono `<reporter> · <created-at>` line and its attachments as upload chips, and the panel follows FOCUS rather than being pinned to the head, so the rail's keys and the panel can never name different issues. The three verdicts become **keys with words** — `[A] Accept`, `[R] Route`, `[D] Decline` — each a real button whose accessible name is the word, its keycap carried by `aria-keyshortcuts` and Decline's landing status by `aria-describedby`. `Route` opens the page's **one transient**: a labelled `role="dialog"` panel naming the issue, five value rows, `⏎` committing all five in one write, `esc` writing nothing and handing focus back. **`issue.routeIssue` gains `projectId`** — the one place the mock drew beyond the shipped mutator — existence-checked in the workspace with **no cross-team rejection**, because a project spans teams, folded into the same atomic `issue.update` that clears `needs_triage`; `canWrite` → `loadIssueForWrite` ordering is untouched, so a viewer is still refused before the project is looked up. The empty state is the mock's second frame — the done disc, `Nothing waiting.`, an onward foot — rendered **only on a complete result**, because an inbox that has not finished syncing is not an empty inbox. Everything the mock folded stays folded and is written down: no SLA or queue-age target, no overdue mark or colour ramp, no triage owner, no suggested label, no duplicate hint (`issue_link` is issue→pull_request only), no per-person throughput, no moved-to-triage time. Contrast in all six blocks moved every mono fact on the panel off `--text-3`, which measures 2.43–3.33 on the panel's tint. **No new table, no migration, no new named query, no new permission path** | ✅ built & archived (PR #42) |
| 37 | `cycles-register` | **The richest un-designed surface, and the one with the most tempting trap.** Cycles was a 256px cycle rail beside one featured cycle — a status pill, a percentage progress bar and that cycle's issue list — which is Home's hero redrawn beside a lens the issue list already owns. This change rebuilds it to `destinations/cycles.html` as **THE REGISTER**: one row per cycle, newest first, each stating a cycle-status glyph drawn on the ISSUE glyph's 20-grid (dashed ring, half arc, filled disc carrying the shipped `DONE_CHECK`), the mono key, the name, the dates, a **scope ledger** that is Home's own scope band at row scale — extracted into one `buildScopeBand`, so the two surfaces are incapable of disagreeing about a cycle — the carry fact, and the two **artifact chips**, drawn only where a stored digest is ready with content or a retro is closed. Below it, **CARRIED IN**: the work that survived a cycle boundary, which no other surface in the product shows, drawn from `carryover_count` ALONE with the ONE origin `rolled_over_from_cycle_id` still names — never a hop inferred from cycle ordering. **The trap refused, and stated once on the page: no burndown.** There is no issue status-history table; `last_human_status_at` is a single scalar and cannot reconstruct a series, so any line falling over time here would be an invention — and with it go velocity, capacity, forecast and every per-person number. **The committed denominator degrades rather than lies**: completing a cycle overwrites the stamp naming the cycle an issue left, so only an open cycle and the latest completed one can still claim a total; every earlier row reads `N landed`, draws no remainder, and the rule is one `how ·` away. The featured cycle's issue list is the one deliberate removal (Issues already filters by cycle), recorded as a MODIFIED requirement; `new-cycle`, `complete-cycle`, `cycle-retro-link`, `cycle-open-retro`, `cycle-digest` and `pm-digest-share` survive verbatim. Contrast in all six blocks moved the selected row's mono key off `--accent-strong`, the register's dates off `--text-3` and the deep-carry count off the amber it is washed in. **No new table, no migration, no new named query, no new mutator** | ✅ built & archived (PR #43) |
| 38 | `projects-roadmap-daylight` | **Two readings of one entity, over one reconciled fixture.** Projects and Roadmap were the two `more▾` destinations (`g p`, `g m`) the overhaul never drew, both wearing pre-overhaul interiors inside the shipped frame. They ship as ONE change because `DESTINATIONS.md` §1 caught the two mocks describing two *different* workspaces at the same instant and closed it by making `roadmap.html`'s nine projects canonical — and the code has the same coupling: one derivation module (`apps/web/src/projects/model.ts`), one e2e spec, and the roadmap's `⏎` opens the project page this change creates. Projects stops being a master–detail rail that lists every project twice (a 256px rail beside a detail pane repeating one of them) and splits into an **index that is a progress reading of all nine** and a **project page worth a URL**. Four honesty fixes travel with it: the progress bar is inked `--status-done`, not the brand accent, because DESIGN's rule is that the accent is never a status; `62% done` over a project with no issues read `0%` — a lie about a project nobody has broken down yet — so the meter carries `done/total` and a project with no issues draws **no ink at all**; the roadmap's per-row italic `No target date` ornament lifts to one group header and those rows get their real content back; and both surfaces now **state that a project is workspace-scoped** while the deck above them is team-scoped, which a reader was otherwise invited to get wrong, since one project can hold issues from several teams. The roadmap's axis stops drawing generic month gridlines and draws the team's actual rhythm off the real `start_date` / `end_date` columns. **No new table, no migration** | ✅ built & archived (PR #46, #47) |
| 39 | `retros-room` | **The one surface allowed to be 10% more human, and the licence that allows it.** `plays/PLAY-warmth.md` sets the condition — every warm move must restate a hard guarantee — so the room's warmth is the voice of the architecture rather than decoration. The model sentence was already true of the schema and nowhere on the screen: *cards are anonymous by design; there is no author column*, true because `retro_card_author` is a **server-only table the Zero schema cannot name**. It replaces a bare `Anonymous` pill, which stated a setting without the reason it can be trusted. Four drawing fixes come with it: the phase machine stops reading as six pill segments in a rounded tray and becomes the **cycle day-band language** the rest of the product already uses for time passing, which is what a retro's phases are; an unvoted card stops drawing a mono `0` and a `−`/`+` pair, which violated `reality-vocabulary`'s shipped "a slot with no fact draws no ink" on most of its rows; the dot budget draws spent and unspent dots **and** keeps the `2/5` reading, so it is never colour- or shape-only; and the tabletop becomes one — flat 3px paper on `--bg-sidebar` felt with a single hairline of thickness at the foot, the half of `plays/warmth-retro`'s paper grammar that graduated. **No new table, no migration** | ✅ built & archived (PR #45) |
| 40 | `board-daylight` | **The one surface in the product that could not tell you an issue was diverged.** Board is the Board lens on Issues, and `issues.html` drew the toggle while the board behind it was never drawn at all. `BoardCard` was handed a hardcoded `buildRealityShape(null)`, so **every card on every board drew the quiet track, whatever git said** — no phrase, no `//` divergence break, no review age — on the surface whose silence guts the product's whole wedge. Band 2 was `Masthead title="Issues"` and the lens toggle and nothing else, so switching from List to Board silently dropped seven filter axes and the saved views. `w-72 shrink-0` inside `overflow-x-auto` put six 288px columns into 1440px of viewport, leaving Canceled off-screen at the width the product is designed for; the six columns are now fluid and readable at 1440 with **no horizontal scroll and no breakpoint**, and a column that overflows scrolls rather than folding, because a folded remainder would hide drop targets from a move. An empty column is **reserved, not captioned**. And the drag becomes legible at rest rather than only in motion: a drawn hole where the card left, a drop slot where it will land, and the `space` / `esc` / `← →` contract stated on screen — which under `prefers-reduced-motion` was close to invisible. **No new table, no migration, no new named query** | ✅ built & archived (PR #44) |
| 41 | `inbox-daylight` | **The last of the seven destinations.** Every other page had come to share one row anatomy, one word diet and one empty-state register; `/inbox` had not. The row led with the sentence and buried the subject — `Marta mentioned you in ENG-116` as the headline, the issue's own title demoted to a 12px subline — and it inverts: the stored `subjectTitle` is the title, and the actor-and-verb is the phrase at rest in the column `issues.html` reserves for it. The four kinds differed only by a verb inside a sentence, and now carry **four glyphs drawn in the house manner** (20-unit box, 1.6 hairline, `currentColor`), so the kind is legible before the words are read — and the borrowed `lucide` bell beside the title is gone, because a borrowed icon set has no place in a drawn vocabulary. The empty state stops being a two-sentence explanation of what an inbox is: the state a good inbox is in most of the time is composed, not apologised for. `j`/`k`, `⏎` and `e` all worked and none was discoverable; a legend footline says so. **No new query, no new subscription, no migration** | ✅ built & archived (PR #48) |
| 42 | `render-defects-cleanup` | **What looking found, the second time.** Two of the three were found by starting the app, seeding it, opening the page and looking at it — neither catchable by an assertion, both having passed every gate on the way in. **(1)** Triage's decision panel reserved its 660px prose measure over an issue with **no description** — the common case for a terse bug report — leaving a ~150px empty region with the three verdicts stranded on the right. The shipped `triage` spec forbade *placeholder text* for that case; nothing forbade the panel reserving the space anyway. **(2)** Delivery's OPEN TO MERGED distribution ran its two callouts together as `…merged inside 26h8 changes waited 110h or more`. `DistributionStrip` *had* a collision test and it did not fire, for two compounding reasons: it demanded **strict geometric overlap** with no minimum gap, so two mono sentences 10px apart were declared apart; and `NOTE_CHAR_W = 6.4`, documented as "deliberately generous", was not — both mono faces advance 6.6px at 11px, and the **editorial preset maps `--type-mono` to Inter Variable, a proportional sans**. Reproduced numerically against the shipped code: the two spans compute **10.2px apart**, under two characters. **(3)** `team.key` collided intermittently because it was derived from a **prefix of a UUIDv7 — whose leading characters are a millisecond timestamp, not entropy** — so the prefix repeats for every id minted in the same bucket, and the bucket widens as the prefix shortens. The fix is one shared `newKey` derivation from cryptographic randomness in `packages/schema`, and the rule is now a `local-first-sync` requirement rather than a lesson: no value whose purpose is uniqueness is derived from a prefix of an id. **No new table, no migration** | ✅ built & archived (PR #49) |
| 43 | `e2e-multi-context` | **The Playwright suite is not deterministic, and `main` is red because of it.** Eleven spec files open 17 `browser.newContext()` sites by hand and tear them down themselves; `projects.spec.ts`'s two-client convergence test fails on `main`, `retro.spec.ts`'s palette test is flaky, and `browserContext.close: Protocol error (Target.disposeBrowserContext)` is the recurring signature — a flake `inbox-daylight`'s own tasks named and explicitly declined to fix or to loosen. Scoped **measurement-first**: run the three suspect spec files five times each, count `reloading in` and `Ignoring mutation … already processed` per file across a passing and a failing run, read one failing trace, and pull `SchemaVersionNotSupported` counts from the last three CI e2e jobs — because D3's CI-ordering fix is taken **only if the count shows the race is real**, a fix that would otherwise mask rather than cure. Then: `onClientStateNotFound` and `onUpdateNeeded` passed from `ZeroRoot`, so an unknown client recovers through the shipped path and a version mismatch surfaces to the user instead of the client silently reloading; all 17 `newContext` sites migrated onto a fixture whose teardown Playwright owns, with a repository gate that fails a direct call; and a watcher that fails any test whose page reloads itself. **No product behaviour change beyond the two sync handlers, no migration** | ❌ superseded by `e2e-determinism` (45) — its reload-chain hypothesis was falsified by the measurement it asked for; proposal and design kept in the archive as the record of that falsification |
| 44 | `decision-record` | **The work graph remembers what, and never why.** Deferred at `team-home-digest` (29) scoping, and the reason `app-frame` (31) folded a Decisions stop out of the deck's `more▾` transient — the stop exists in the drawing and has no entity behind it. A team decides things in a retro, in a triage route and in a comment thread, and the record lives wherever it was typed, so the DECIDED THIS CYCLE band Home wants to draw has nothing to read. Adds the `decision` entity, Decide-in-composer, the band, and a Decisions record page. **New table, new migration** | 📋 scoped (proposal merged, PR #50) |
| 45 | `e2e-determinism` | **The fourth diagnosis, and the one that held.** Three changes were scoped against one flaking two-client test — accumulation (PR #41), a reload chain (43), and finally the truth: an unbounded action waiting on a transient that never opened, reported sixty seconds later as a teardown error three frames away. The harness could not report where it failed, and that misreporting manufactured both wrong theories. Fixed at every layer it touched: bounded actions that fail at their own selector; `goToMore`/`signOut` assert the transient open (idempotent, `aria-expanded`-keyed); the suite serves the built bundle from the app server itself — one origin, the production shape, no dev optimizer; the sign-up redirect-cycle renderer freeze (RC1), the deferred-server-ack races (RC3) and two test bugs fixed at their mechanisms; `ZeroRoot` supplies `onClientStateNotFound` and `onUpdateNeeded`, so the library’s silent `location.reload()` is unreachable — an unknown client resyncs visibly, a version mismatch offers the user a Refresh instead of taking the page mid-write (a real gap, and measurably **not** the cause of the flake); every `browser.newContext()` migrated onto a Playwright-owned fixture with a boundaries gate (carried from PR #41, with credit and its diagnosis restored); an unrequested reload fails the test that saw it, naming the reason; workspace growth is annotated per test and budgeted at run end; and the full suite proven 95/95 on three consecutive fresh databases against a control that failed every run. **No migration; product changes confined to the sync-recovery handlers and the RC1/RC3 fixes** | ✅ built & archived (PR #53, #54) |
| 46 | `front-door` | **The front door opens into the utility closet.** After sign-in a member lands on `/`, which renders workspace administration — Members, Teams, Invitations — while the morning digest, the product's best surface, sits one click behind it. Redirects sign-in to the anchor team's Home, keeps `/` as the honest zero-team landing, and stops the Issues list opening on an archive (57 issues, 54 of them Done). Three facts corrected during authoring make it larger than one line: there are **two** redirects, not one — `login-form.tsx`'s `callbackURL` sends sign-**in** through better-auth's redirect plugin while sign-**up** goes through the route — `queries.teams.all()` returns every workspace team to any member, so the deck's anchor can name a team the caller has no membership in and a *redirect* needs a stricter test than an *offer*, and the decision must wait for the roster or a member with five teams lands on administration. First of the legibility family (`openspec/SCOPE-legibility.md`), sequenced **before** planning-surfaces. **No migration; no new entity** | 📋 scoped (proposal authored) |
| 47 | `explanation-at-rest` | **The product explains itself in permanent UI instead of teaching once.** `delivery-metrics/spec.md:271` already says a derived number "SHALL carry a quiet `how ·` … and SHALL carry no other explanation at rest"; the rule is right and enforced on two surfaces out of nine. Team Home prints `yours = assignee you · status < done · …` and `composed = attention first · …` on every visit, and Projects prints its counting rule **directly beside the `how ·` built to hold it**. Lifts the prohibition into `reality-vocabulary` and folds four query-definition footnotes behind the affordance. The interesting half is the boundary it draws and ships as its own requirement: **`how ·` explains derivations, and is not a drawer for everything a page says** — refusals, the mandated metrics promise (`app-frame:229`), section standfirsts (`delivery-metrics:263-266`) and live-session retro copy all stay at rest, because `how.tsx` renders `{open ? … : null}` and folding takes text out of the DOM rather than merely quieting it. **Reverses no recorded position. No migration** | 📋 scoped (proposal authored) |

**Where v1 actually stands.** The **PM core** (changes 1–7 plus `design-system`) is complete: workspace/auth, design system, issues, board, cycles, triage, and projects with a roadmap. With `connectors` (8) the delivery-truth wedge is live — GitHub PR/CI/deploy state feeds the issue-row reality track and its `//` divergence break. With `ai` (9) the BYO-key AI foundation and its first flagship, the team-internal cycle digest, are in. With `retro-board` (10) the team has the surface where it decides what to change, shipping with no AI in it at all. With `notifications` (11) the work graph can finally reach a person: a self-scoped inbox, and the provider-neutral mailer the rest of the product's outbound email now goes through. With `mentions` (12) a person can be reached *by name*, and the issue they were named on keeps reaching them until they say stop. With `search` (13) the last locked-scope surface is in: instant on-device, completed by Postgres full-text, in two labelled groups — and with no query recorded anywhere. With `auto-status` (14) the wedge line's middle clause is true for the first time: a linked PR opening or merging actually moves the issue, for the teams that ask for it — and for every team that does not, the `//` divergence break remains exactly what it was, which is the whole reason the switch exists. With `editor-markdown` (16, built ahead of 15) text can finally *leave* yapm and still read like text: markdown on the plain-text clipboard, markdown accepted on paste, and rich text still the only thing stored. With `attachments` (15) and `editor-rich-content` (17) the editor holds more than paragraphs — images, tables and highlighted code, a `/` insert menu, and a Files list on every issue — and it holds them without ever putting a fetchable URL in a synced document, and without a stale tab being able to delete them. With `retro-ai-draft` (18) the first AI *feature* on the change-9 substrate is in, and it lands where the stakes are highest: a model drafts wins, losses and improvements into a retro that sits beside a board of anonymous cards — reading none of them, no comment, and no identity column anywhere, generated lazily at the reveal so there is nothing to anchor on and nothing to spend on a retro nobody runs, off per team until an admin says otherwise, and labelled as unratified. With `retro-ratification` (19) that label finally has something behind it: each member's agree/disagree is synced to nobody but themselves — not to a teammate, not to a workspace admin — the verdict and counts are computed ONCE, server-side, at the `vote → discuss` advance so no counter exists for concurrent reactions to contend on, a single disagreement makes a proposal contested under a rule with no knob to turn, and an agreed improvement is one keystroke to the shipped conversion path — which forwards a null assignee, because a suggested owner would be the first per-person output in the entire AI layer. With `retro-ai-loop-close` (22) the retro finally compounds: the draft for cycle N can report on up to three of the improvements the team agreed in its most recent previous retro — naming the cycle they came from — saying whether each shipped, was canceled, is still open or was never tracked, yapm's own four words computed from the live status of the issue each action became, never a phrase the model chose — read through the one place the fact assembly touches an identity-bearing column, with BOTH `assignee_id`s stripped by column allowlist and asserted against the built object rather than the prompt. It cost no migration at the time (the fourth group was a bucket derived over a new reference kind, and the first-retro absence fell out of the shipped cite-or-omit validator) — a decision **reversed by `retro-followup-category` (24)**, which makes `follow_up` a stored fourth category under migration `0022`, deletes the derivation outright rather than leaving it beside the stored value, and restores "no prior actions ⇒ no follow-up" as an explicit validator instead of a side effect. It brought the operator's half with it: an admin-only, read-only log of what teams agreed, contested and threw out — the only evidence a running instance produces that the drafts are worth reading, team-level by construction and never fed back to the model. With `pm-digest-boundary` (20) the first yapm output to cross a permission boundary shipped *with* its boundary — a separate synced row rather than a widened read, four independent switches all defaulting off, evidence baked as plain text a reader outside the team could not open anyway, and a server-only audit table excluded from the sync schema entirely — and with `pm-digest-areas` (21) it acquired something to say, converting file paths into product areas under the GitHub permission already granted, without yapm ever reading a diff. With `pm-digest-governance` (23) the AI-features family closes, and the two words change 20's mission forbade become true rather than aspirational: the disclosure log is **retention-bounded** by a nightly sweep on the pg-boss instance that already exists (365 days, configurable, and running whether or not AI is enabled — a bound that lapses when the feature is switched off is not a bound), it is **auditable** through an admin-only view that reports what was disclosed and to how many readers and structurally cannot report a read, and a named reader can be told a digest is ready by an optional email carrying **a link and nothing else** — because a mailed artifact sits outside the kill switch, outside retention and outside the audit log simultaneously, which makes link-only a stated decision rather than a property somebody happened to preserve. It cost no migration and no container. What none of it undoes is stated in change 20's own words and left in them: retraction stops further reads, it does not un-read. With `deployment-hardening` (25) the part none of the above was any use without is finally true: the documented quickstart reads the operator's own `.env` instead of silently applying the secrets this repository publishes, a production boot on one of those secrets is a refusal that names it rather than a warning nobody reads, a published image resolves the browser's sync origin at runtime so it can sync on a host other than the one it was built for, images publish on every push to `main` whether or not release automation succeeded, and the upgrade path — with its forward-only, restore-from-backup rollback — is written down for both deployment shapes. None of it is a feature; all of it is the difference between a product that demos and a product that can be run.

**And then the product was made to look like itself.** With `sso-admin-gating` (26) the one configuration surface that had never been admin-gated is — on a public instance anyone who could sign up could bind their own IdP to any email domain, after which yapm itself would redirect that domain's real employees to an attacker's authorization endpoint — and the login form stops offering a Continue-with-SSO button on the many instances that had no way to register a provider at all. With `deploy-history-edge` (27) the last edge in `issue ↔ PR ↔ CI ↔ deploy` becomes durable: a write-once deploy timestamp and the merge commit on both sides of the join, so `merged-not-deployed` — selectable, spec-sanctioned and unconditionally empty since it shipped — finally evaluates, and a row can say a change actually reached production. With `team-delivery-view` (28) the five flow metrics behind half the README's headline promise get a home: they were computed and tested and reachable at exactly one URL, inside a single retro, scoped to one cycle, and they are now a rolling window of completed cycles at `/teams/{teamId}/delivery` — no formula duplicated, no query surface added, team-level at every depth. With `team-home-digest` (29) the URL a teammate opens first every morning stops being a members roster and becomes the morning digest, composed entirely from rows already on the device: one attention number over four disjoint exception classes, a literal trailing-24h band, your own work under a lens that never compares you to anybody, and every empty band absent rather than apologetic. And with `inbox-daylight` (41) the **seven-destination set closes**: changes 30–41 took the product from a shipped vocabulary nothing consumed to every destination drawn — one row anatomy, one word diet, one empty-state register, and one rule underneath all of them, that a surface may state only what it can prove. Twice now that rule has been enforced not by a test but by somebody opening the page and looking at it — `design-corrections` (35) and `render-defects-cleanup` (42) are both changes whose entire content is what looking found, and neither defect was reachable by an assertion. What remains unlooked-at is tracked as such.

**Changes 30–35 are one body of work, and they are the reason the product now reads as one product: it draws delivery reality in ONE vocabulary, inside ONE frame, and states it in words.** `one-reality-vocabulary` (30) settled the drawing — the 86px strip of lucide icons is deleted, the four-station track wins, the `//` divergence break becomes a segment kind positioned by which divergence fired, status arcs and priority ticks are reconciled to one geometry, and the peek, the `how ·` dot and the provenance mark land as shared patterns with no consumer yet. `app-frame` (31) settled the chrome — the three-band frame (48px deck, page-owned masthead, 32px statusline), six deck stops with `more▾`, one global `⌘K` owner in place of four `window` listeners, one attention derivation the badge, the statusline and Home all read, and Board demoted from a peer destination to a **lens** in the Issues masthead. `issue-list-daylight` (32) made the row *say* what it draws, from ONE shared phrase dictionary in two registers, behind a quiet filter bar that lost no capability and a fold that states the true remaining count. `issue-detail-bridge` (33) gave the page named after yapm's whole argument the argument itself: two registers (a plain line for a PM above a mono line for an engineer, from one signal and one timeline), the vertical rail, and a divergence callout carrying two clocks as evidence and two real actions — with four blocks folded away because nothing backs them. `delivery-journalism` (34) rebuilt the Delivery view as a journalism cut where every section leads with what the data says, and corrected the honesty line rather than porting it, because the mock's claim had stopped being true. `design-corrections` (35, in flight) is what looking found: quiet rows draw no ink at all, `done` gets its check back, band 3 says `Synced`, and the in-progress amber is retuned to clear 3:1 on all seven grounds — the one place where the mock is wrong and the product is right, recorded as such in `NORTHSTAR.md`. None of the six added a table, a query, a mutator or a migration.

**V1 is now complete.** Three things the locked scope above names had never been built, and saying "V1 PM core complete" obscured them. All three are now closed:

- **Notifications** — "opinionated defaults" commits to "in-app inbox + email for mentions/assignments". Closed by changes 11 and 12: the in-app inbox, the assignment and comment triggers, and batched email ship in 11 (and `SMTP_URL` finally has a consumer); the *mentions* half of that sentence ships in 12, which turned out to be more than a notification kind — auto-subscribe made it a new synced entity with its own migration.
- **Search** — "Surfaces" commits to "search/filtering". Filtering shipped in `issue-core`; search did not. Closed by change 13: an on-device pass that answers on the keystroke with no network, a Postgres full-text pass that extends it with comment bodies and other teams' issues, and a `/search?q=` route beside the palette. The gap's own evidence — `grep -rn "ilike|tsvector|to_tsquery"` over `apps` and `packages` returning nothing — no longer holds.
- **PR state driving issue status** — the "Wedge proof" line above has promised it since day one. `connectors` built the road up to the last inch: a PR is linked, and `computeDivergence` *flags* a status that disagrees with git rather than correcting it. Nothing wrote a status. That gap had its own evidence too — `grep -rn "auto-status|automatic status" apps packages` returned nothing — and it had produced a repo that contradicted itself in four places at once, with the wedge line promising transitions while both ROADMAP §Post-v1 and VISION §Phase 2 listed them as unstarted Phase 2 work. Closed by change 14, on the maintainer's terms: **opt-in per team, off by default, divergence unchanged when off.** All four sentences now agree.

Changes 11–13 closed the first two. They were scoped — sequence, overlaps, constraint checks, falsifiable checks, and the decisions that need a human — in [`openspec/SCOPE-v1-gaps.md`](openspec/SCOPE-v1-gaps.md), which was the mission input for those three build flows. Change 14 was scoped on its own, against the archived specs, because its gap was a contradiction in the docs rather than a missing surface. After it, what was left was AI *features* on the change-9 foundation (the governed PM-facing digest, the **AI-facilitated** retro drafting on top of the retro that now exists) rather than core project management. That family — changes 18–24 — **is closed**; it was scoped in [`openspec/SCOPE-ai-features.md`](openspec/SCOPE-ai-features.md) against the archived `ai` and `retro-board` specs **and against the code** — which contradicts the pre-change-9 research in a dozen places, most consequentially that there is no PR body, no labels, no commits and no diffs anywhere in the schema, that ZQL has no `select()` so a PM-audience read cannot be a column projection of the team's own digest, and that no audit table exists at all. Its headline finding is that **the shared substrate does not need a shared change**: change 9 already shipped it, the genuinely-missing shared work is under 100 lines, and change 18 owns it. One thing is **declined rather than deferred**: putting patch content in front of the model. It would need a secret scanner that does not exist in-stack, and it is the only proposal in either family that inverts a shipped spec guarantee — with the output crossing a permission boundary, `ai-agent`'s "the worst case is a bad paragraph, never a leak" stops being true. Paths→areas (change 21) carries the product claim without it.

Order rationale: schema-risk first (2–3 define the work graph's core tables), daily-use surfaces before periphery, connectors before AI, and both last because they bind to stable issue semantics and a rich work graph. `design-system` was inserted after workspace-auth (design pass before the first product screens). Within 11–13: `notifications` owns the write seam and the mailer both others depend on; `mentions` then uses that seam — planned as a new `kind` and nothing else, until the maintainer's answer on auto-subscribe added a durable `issue_subscription` table and migration `0014_mentions` (so `search` takes `0015`); `search` is last because its plaintext walker extends the mention-aware one `mentions` already shipped in `packages/schema/src/rich-text/`, and its central risk (write amplification, and zero-cache replicating past a large GIN index) cannot be measured until it exists. Both resolved when it did: the H10 answer removed write amplification entirely by moving index maintenance off the write path, and zero-cache was verified to replicate cleanly past `search_document` and its GIN expression index from empty volumes, with no publication change.

## Known gaps

Tracked so they are not forgotten — promises made in prose that the code does not yet keep.

- **One-command export. STILL OPEN.** README ("Your data. One-command export, no lock-in") and VISION #5 both promise it; nothing implements it. TECHSTACK's self-host section makes the adjacent commitment (`yapm backup` → pg_dump + attachments tarball, "documented from day one") — also unimplemented. These are one change, and "free means free" leans on them: export is what makes the no-lock-in claim verifiable rather than rhetorical. Half its substrate now exists: `attachments` (15) wrote the backup contract down and shipped it as a documented manual procedure — what a complete backup contains per provider, the database-then-files capture order (which the restore repeats, for the same reason in the other direction), and the `attachment` table as the manifest an S3 operator verifies their own bucket backup against (see [Backup and restore](apps/docs/src/content/docs/self-hosting/backup-restore.md)). **Change 16 (`editor-markdown`) shipped and does not close this either.** It built and proved the rich-text→markdown serialiser an export needs, and deliberately built no export surface: no `/api/v1/**/export.md`, no `yapm backup`, no zip, no bulk, no CLI, no server-side markdown rendering. One constraint it *did* settle for whoever picks this up — the serialiser lives in `packages/ui`, so an export that runs on the server cannot import it as it stands; that is a real cost of keeping the TipTap graph out of `apps/server`, and it is the right trade. **Change 17 (`editor-rich-content`) added serialiser cases for tables, images and fenced code and closes none of it either** — for the same reason, and with the same constraint: it also lives in `packages/ui`, and an image's markdown path is computed by the caller, so an export running anywhere else has to supply one. What is left for the change itself is the command that automates them. **Still unscheduled.**
- **The product ships two visual registers. OPEN, one destination narrower.** Changes 30–35 rebuilt five surfaces — the frame, Team Home, the issue list, the issue detail and Delivery — to the settled vocabulary in [DESIGN.md](DESIGN.md), and changes 36 and 37 rebuilt Triage and Cycles, the first two of the seven un-drawn destinations. **Five destinations reachable from the same deck still carry the pre-overhaul chrome**: Board, Retros, Projects, Roadmap and Inbox (`apps/web/src/board/`, `retro/`, `projects/`, `notifications/`, and the `search/` and `settings/` surfaces beside them) — outlined `lucide` buttons, no phrase at rest, no shared glyph geometry. A user who walks the deck from Home to Board still crosses a visible seam. Their mocks are drawn (`design-explorations/overhaul-2026-08/destinations/`); the gap is the four rebuilds against them. Board is the sharpest of the six, because it is a *lens on Issues* and sits one keystroke from a list that has already been rebuilt — and its card draws neither the reality track nor the phrase at rest.
- ~~**Attachments.**~~ **Closed** by changes 15–17 (see [SCOPE-editor-attachments.md](openspec/SCOPE-editor-attachments.md)): storage seam and routes, markdown interchange, then the editor nodes and the Files section. What deliberately stayed out: signed or shareable links of any kind, avatars, a `fileAttachment` document node, and a block editor.

## Differentiation commitments

- **Issue list shows reality, not just intention** — a row with a linked change both **draws** its delivery signal (PR/CI/deploy/review) as a reality track, with a `//` break where the board and git disagree, and **says** it, in words from one shared phrase dictionary (`Checks failing`, `Done in git, not on the board`, `Built — not live yet`); a row with no linked change is genuinely blank, because reserving the slot and laying down no ink is more honest than an ornament repeated on sixty of sixty-nine rows. All of it derived from the work graph; see [DESIGN.md](DESIGN.md). Layout built in `issue-core`; git data populates via `connectors`; with `auto-status` a team can opt in to having the reality *correct* the intention instead of only flagging it; and with `deploy-history-edge` the row can say a change actually shipped, because the moment a deployment carrying its merge commit succeeded is finally stored rather than overwritten.
- **Connectors, not special-cases** — GitHub is the first implementation of a reusable first-party framework; GitLab and others follow as connectors.
- **AI as a moat is AI-over-the-work-graph** — BYO-key, self-hosted, provider-agnostic, operating under the same permission model as humans (which is also the safety story). Architected from the start, shipped once the graph is rich.

## Post-v1 (from VISION.md phasing)

- **Phase 2 — Delivery truth**: CI/deploy ingestion, DORA + review-health + CI-health views (team-level only). Lights up the issue-list reality track's full signal. *(Automatic status transitions were listed here and have moved out: the opt-in, per-team form shipped in change 14. Deploy- and CI-driven transitions remain Phase 2 and are not currently planned — the issue↔deployment edge they needed is no longer the obstacle: change 27 models it as an exact merge-commit join and stores a write-once deploy timestamp, so deployment frequency became answerable. A deploy-driven **status transition** is a separate product decision, declined for the reason change 14 gave about the transitions it did not take.)* **The review-health and CI-health views ship in change 28** as the team Delivery view: PR cycle time, time to first review, review rounds, unlinked issues and the CI failing rate, over a rolling window of completed cycles, team-level only. **The deploy half is now on that view too** (change 34): every deployment that reached production is drawn along the cycle in progress, at the moment it reached it, from the write-once timestamp change 27 stores. What the page deliberately does **not** show is deployment frequency **as a rate** — deployments are drawn as they happened rather than normalised — and it says so on the page. Of the four DORA keys the view carries lead time only partially (open→merge, not commit→deploy); change failure rate and MTTR stay Phase 3 because both need an incident entity that does not exist.
- **Phase 3 — Incidents + cloud**: incident tracking (change failure rate, MTTR), managed cloud offering. *(Two of the four DORA metrics live here and nowhere else: change failure rate and MTTR both need an `incident` entity, and change 27 deliberately declined to half-build one to gesture at DORA. Lead time and deployment frequency are computable from what 27 stores.)*
- Backlog of deliberate deferrals: full multi-color theme editor, "blocked-on" issue axis, estimates, sub-issues, additional connectors (GitLab, Jira/Linear import), true third-party plugin runtime, mobile/desktop apps, i18n, public API expansion, Yjs collaborative descriptions.
