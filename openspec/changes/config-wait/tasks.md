## 1. Read before touching anything

- [ ] 1.1 Read `openspec/changes/front-door/design.md:745-780` — the hand check that produced this change, including its table of what a caller sees at ~25s and ~100s, and both findings it declined. That section is this change's evidence, and the two findings are its two halves
- [ ] 1.2 Read `apps/web/src/zero/runtime-config.tsx` end to end — all 156 lines. Every edit in this change is in this one file. Pay particular attention to the four comment blocks that state rules: `:10-11` (why the fetch is bounded), `:14-18` (why the wait is announced and not drawn — **the position this change moves**), `:79-81` (why the failure waits — **the intent this change keeps and the mechanism it replaces**) and `:144-148` (no spinner — **kept**)
- [ ] 1.3 Read `apps/web/src/zero/backoff.ts` in full and compute for yourself that `atBackoffCeiling` first returns true at attempt **5**, then read `openspec/changes/config-wait/design.md` §D1 and confirm the two distributions. If your arithmetic disagrees with D1, stop and say so before writing code — the whole change rests on those numbers
- [ ] 1.4 Read `apps/web/src/zero/provider.tsx:220-246`. `:232` and `:240-245` are the shape this change copies for its reveal timer, and `:232`'s disjunction is the thing design D6 deliberately does **not** copy. Understand why before editing
- [ ] 1.5 Read `apps/web/src/zero/recovery.ts:1-18`. `RETRY_OFFER_AFTER_MS` at `:9` is the constant this change imports; `:1` is a type-only import and `:2` is React, which is what makes importing this module from the boot gate safe (design D2, and the gate's whole purpose in `apps/web/src/main.tsx:25-27`)
- [ ] 1.6 Read `openspec/specs/self-host-deploy/spec.md:495-539` — the requirement this change modifies — and `openspec/changes/config-wait/specs/self-host-deploy/spec.md`. The delta restates the requirement **whole**, including two paragraphs about build-time constants and the development default that this change does not touch. Confirm for yourself that those paragraphs are carried verbatim
- [ ] 1.7 Read `openspec/changes/explanation-at-rest/specs/reality-vocabulary/spec.md:94-96`. This change adds visible text in a family whose purpose is removing it; that clause is why it is allowed, and design D3 is the argument. Do not add a second line, a heading, or a sentence explaining the wait

## 2. The reveal moves from the attempt counter to the clock (`apps/web/src/zero/runtime-config.tsx`)

- [ ] 2.1 Import `RETRY_OFFER_AFTER_MS` from `@/zero/recovery` and drop `atBackoffCeiling` from the `@/zero/backoff` import at `:3`. `backoffDelay` stays. **Do not edit `backoff.ts`**: `atBackoffCeiling` keeps its caller at `provider.tsx:232` and its tests at `backoff.test.ts:62-65`
- [ ] 2.2 Hold the timestamp of the **first** attempt in a ref, in the shape `provider.tsx:222` uses (`if (startedAtRef.current === null) startedAtRef.current = Date.now()`). First attempt, not most recent — design D9 is why, and a test in §5 holds it
- [ ] 2.3 Replace `if (atBackoffCeiling(currentAttempt))` at `:82` with an elapsed test against `RETRY_OFFER_AFTER_MS`. Rewrite the comment at `:79-81` with it: the intent it states ("a page that says so on a slow first paint is a page that cried wolf") is **kept**, and the sentence now has to say what enforces it, including that a counter cried wolf in the refusal case
- [ ] 2.4 Add the reveal **timer**, or the change does not do what it says. A rejection-keyed elapsed test fires on the next rejection after the bound — ~20.5s against a hung endpoint, not 15s. Schedule it in the load effect in the shape of `provider.tsx:240-245`: `setTimeout(…, Math.max(0, RETRY_OFFER_AFTER_MS - elapsed))`, cleared by the effect's existing cleanup at `:99-102`, and guarded by the same `cancelled()` check every other state write in this file uses
- [ ] 2.4a Say where the timer's `reason` comes from, because the timer fires outside the rejection handler and `reason` is not optional. The `State` union at `:56` requires it, the failure surface renders it at `:134`, and today it is produced in exactly one place — `error instanceof Error ? error.message : String(error)` at `:85`, inside the closure of the rejection that produced it. Have the rejection handler **record** that string in a ref as well as using it, and have the timer read the ref. By 5.5's relation (`RETRY_OFFER_AFTER_MS` 15s > `RUNTIME_CONFIG_TIMEOUT_MS` 10s) at least one attempt has rejected before the timer fires, so the ref is populated in both failure modes — but the read must still be total, so give it a fallback for the case that relation is ever broken, and do not invent a second sentence for the surface to draw (design D9: the copy at `:121-142` does not change)
- [ ] 2.5 `retry` (`:114-117`) resets the attempt counter and **not** the first-attempt timestamp, and does not clear `phase: 'failed'`. Its comment at `:111-113` explains what the reset achieves; extend it to say what the reset deliberately does not touch (design D9)
- [ ] 2.6 `phase: 'failed'` stays set-once. The only thing that leaves it is a successful load reaching `setState({ phase: 'ready', config })` at `:75`. Do not derive the phase from elapsed time on every render
- [ ] 2.7 `fetchRuntimeConfig` (`:37-51`), `RUNTIME_CONFIG_TIMEOUT_MS` (`:12`), `RUNTIME_CONFIG_URL` (`:8`), the load effect's backoff (`:97`) and the failure surface's markup and copy (`:121-142`) are **unchanged**. If your diff touches the words on the failure surface, it is the wrong diff (design D7, D9)

## 3. The wait is drawn as well as announced (same file)

- [ ] 3.1 Remove `className="sr-only"` from the boot shell's paragraph at `:151`. One node, one `role="status"`, one `aria-live="polite"`, one string — the visible line and the announced line are the same DOM text, not two copies that can drift
- [ ] 3.2 The word stays `Loading…` exactly (`:152`). No new phrase, no ellipsis change, no second line, no heading (design D4)
- [ ] 3.3 Give the boot shell the centring the failure surface in the same file already uses at `:124` (`flex min-h-full items-center justify-center`), and give the line `text-muted-foreground text-sm` — the exact classes `apps/web/src/components/authenticated.tsx:43` and `apps/web/src/components/auth/login-page.tsx:83` use for the same word. **No new token, no hardcoded colour, no new component.** `--muted-foreground` resolves to `--text-2` (`packages/ui/src/styles/globals.css:473`), already asserted at AA in all six theme blocks by `packages/ui/src/styles/contrast.test.ts:170-177`, so `packages/ui` is not touched and no contrast pair is added
- [ ] 3.4 `BOOT_ANNOUNCE_DELAY_MS` stays at `1_000` (`:18`) and the announce effect (`:105-109`) keeps its shape: the region is still mounted empty from the first paint and filled a beat later. Rewrite the comment at `:14-18` — it currently says "Nothing is drawn either way … the wait is announced to assistive technology only" and states a reason that holds for one second and not for ninety. Say what the beat is for now (no flash on the ordinary same-origin fetch) and drop the claim about which reader is looking at what
- [ ] 3.5 The comment at `:144-148` keeps its no-spinner rule — it is still true and still load-bearing — but its first sentence describes a shell that draws nothing at all. Correct it to describe what the shell now draws and when, and keep the live-region sentence (`:146-148`) verbatim: that reason has not changed
- [ ] 3.6 `data-testid="runtime-config-boot"` (`:150`) and `data-testid="runtime-config-failed"` / `runtime-config-retry` (`:125`, `:136`) are **unchanged** — they are the contract the tests read

## 4. Nothing else in the app moves

- [ ] 4.1 `apps/web/src/zero/backoff.ts`, `apps/web/src/zero/recovery.ts`, `apps/web/src/zero/provider.tsx`, `apps/web/src/zero/session.ts` are **not edited**. This change reads one constant and amends nothing in the recovery path
- [ ] 4.2 `apps/web/src/components/authenticated.tsx` and `apps/web/src/components/auth/login-page.tsx` are **not edited**. Their `Loading…` surfaces are already visible with a live region; they are this change's precedent, not its subject
- [ ] 4.3 No in-page `Loading…` is touched — `triage-view.tsx:430`, `issue-list.tsx:149`, `projects-view.tsx:172`, `roadmap-view.tsx:154`, `project-page.tsx:167`, `board.tsx:213`, `cycles-view.tsx:156`, `retro-view.tsx:251`, `retros-view.tsx:208`, `inbox-view.tsx`, `issue-detail.tsx`. Design D11 declined all of them. If a diff of this branch touches one, the edit is wrong
- [ ] 4.4 `apps/web/src/main.tsx` is unchanged. The gate stays above `ZeroRoot` for the reason at `:25-27`
- [ ] 4.5 `apps/server/src/app.ts:66` and `apps/server/src/config/env.ts` are unchanged: no endpoint, no environment variable, no default moves. There is no new knob for the threshold, deliberately (design Non-Goals)
- [ ] 4.6 `packages/schema` and `packages/ui` are not touched at all

## 5. Unit and component tests (`apps/web/src/zero/runtime-config.test.tsx`)

- [ ] 5.1 `:156-172` ("the boot wait is announced politely, a beat after it starts") asserts the region is empty at first and holds `Loading…` after 1.5s. It keeps both assertions and gains the half it never made: after the beat the text is **visible**, not `sr-only`. Assert it the way `apps/web/src/triage/triage-view.test.tsx:216` does — a query that excludes `.sr-only` — so the test fails on `main`
- [ ] 5.2 **New: the reveal lands at the bound against a fetch that never settles.** Fake timers, `fetch` returning `new Promise(() => {})` (the shape already at `:158-161`), advance to just under `RETRY_OFFER_AFTER_MS` and assert `runtime-config-boot` is still what is rendered; advance past it and assert `runtime-config-failed`. **This is the test a rejection-keyed implementation fails** (design D6), because with a 10s request timeout no rejection has occurred at 15s
- [ ] 5.3 **New: the same bound in the refusal mode.** `fetch` rejecting immediately, advance to the bound, and assert the surface appears at the same point — not at whatever the jittered gaps happened to sum to. Seed `Math.random` or stub it, because `backoffDelay` takes its randomness from `Math.random()` by default (`backoff.ts:16`) and an unseeded test asserts a distribution rather than a rule
- [ ] 5.4 **New: the endpoint is never named before one bounded attempt has failed.** Advance to just past `RUNTIME_CONFIG_TIMEOUT_MS` with a hanging fetch and assert the failure surface is absent
- [ ] 5.5 **New, and it is a constant test rather than a behaviour test:** assert `RETRY_OFFER_AFTER_MS > RUNTIME_CONFIG_TIMEOUT_MS`. The relation is invisible across two files and 5.4 silently degrades to a tautology if somebody raises the request timeout (design D5)
- [ ] 5.6 **New: a failed retry does not hide the failure.** Reach the surface, press `Retry now` against a `fetch` that still fails, advance a little, and assert `runtime-config-failed` is still on screen and its button still there (design D9). The existing success path at `:128-154` already covers the press that works
- [ ] 5.7 **New: a late success clears the surface with no reload.** Reach the surface, start serving a valid config, advance past one backoff step **without pressing anything**, and assert the app mounts. This is the property that makes an earlier reveal safe (design D5.3) and nothing asserts it today
- [ ] 5.8 `:108-123` and `:128-154` each walk `BACKOFF_CAP_MS + 1_000` ten times to reach the failure surface (`:115-119`, `:133-137`). They stay green unchanged — ten × 31s clears any bound — but simplify both loops to advance past the bound once, and keep every assertion they make. What they prove (no silent default; a landmark, a live region and a keyboard-reachable retry) is unchanged by this task list
- [ ] 5.9 `:86-106` ("constructs no Zero client, and shows no error, while the config is in flight") must stay green **untouched**: it asserts `runtime-config-boot` is present and that `RUNTIME_CONFIG_URL` appears nowhere on screen. A visible `Loading…` does not name the endpoint, so it still passes — confirm rather than assume, because if it fails the line being drawn says too much
- [ ] 5.10 `:72-84` (the headline check) and `:176-195` (no `VITE_ZERO_CACHE_URL` on the connection path) are untouched and must stay green

## 6. E2E — one spec, argued rather than added reflexively

- [ ] 6.1 Read PROCESS.md §3 before writing it. The big-feature rule ("a change needs all three tiers iff it touches ≥2 of {synced entity/schema, mutator, permission surface, signature UI} … Otherwise it is small: unit + integration only; do not add E2E reflexively") makes this a **small** change: it touches none of the four. There is also no integration tier here — no database is involved. The case for one spec anyway is that this defect was only ever found by hand against a live stack, that PROCESS §3 already lists "a failing or hung sync-credential request" in the E2E tier as the same class of thing one layer up, and that jsdom cannot observe a real fetch abort against a real server. Write **one** spec, and if that argument does not convince you, say so in `design.md` under "Decisions made during implementation" and write none
- [ ] 6.2 Add it to `apps/web/e2e/reconnect.spec.ts`, modelled line for line on `:354-374` ("a hung sync-token request is abandoned for an actionable retry"), which already routes a URL to hang for 20s and asserts the actionable surface. Import `test` from `./fixtures` as every spec must (PROCESS §3's lifecycle contract) — the file already does at `:2`
- [ ] 6.3 The spec calls **no** `ensureAccount` and **no** `openWorkspace`: route `**/api/config` to hang, `page.goto('/')`, assert the boot line, assert the failure surface, unhang, press `Retry now`, assert the app paints. Needing no account is itself the evidence that this gate sits above authentication (design D2)
- [ ] 6.4 Assert both halves: that `Loading…` inside `[data-testid="runtime-config-boot"]` is **visible** (`toBeVisible`, which is exactly what an `sr-only` node fails), and that `[data-testid="runtime-config-failed"]` appears within a bound comfortably above 15s and comfortably below the ~91s this change removes — 25s is the honest window and it fails on `main`
- [ ] 6.5 Set the test's own timeout explicitly, as `:355` does. Do not reload the page anywhere except where the test itself asks (`:368`'s precedent) — an unasked reload fails the test that saw it
- [ ] 6.6 Nothing else in `apps/web/e2e` changes. Grep the directory for `runtime-config` afterwards and confirm this new spec is the only reference

## 7. Documentation

- [ ] 7.1 `apps/docs/src/content/docs/self-hosting/sync-recovery.md` §"What people see" (`:16` onward): the state table at `:24-34` covers every state a signed-in browser can show. The one before the app knows *where* sync lives is missing, and it is the only one that can appear to a caller who has not signed in. Add it near the paragraph at `:46-50`, which describes the credential path's full-page retry as though it were the only one — two sentences at most, naming what the screen says at a beat and what it says at fifteen seconds
- [ ] 7.2 Same file, §"When it stays reconnecting" step 2 (`:138-141`) already tells an operator to `curl -s http://your-host:3000/api/config`. It now has a symptom to hang that instruction on — a browser that shows "yapm can't reach its own configuration" is telling them the app process, not `zero-cache`, is the thing that is not answering. One clause, cross-referenced, not a new section
- [ ] 7.3 `apps/docs/src/content/docs/self-hosting/deploy.md:193-197`: the "Getting it wrong looks like" table says a wrong `ZERO_CACHE_PUBLIC_URL` means "The app renders, and stays empty". An unreachable `/api/config` looks nothing like that and is not in the table. The `:199-208` paragraph below it already explains the endpoint; make the distinction visible where an operator is diagnosing
- [ ] 7.4 The word diet applies to these pages too. Do not write a paragraph explaining a fifteen-second threshold to a self-hoster; write what they see and what it means
- [ ] 7.5 No other doc is made stale, and confirm rather than assume: no environment variable, container, endpoint or command changes, so `.env.example`, `README.md`, `TECHSTACK.md`, `SECURITY.md`, `DESIGN.md` and `CLAUDE.md` are all untouched. `apps/server/src/config/env-example.test.ts` asserts the env/compose/docs set equality and must stay green with no edit
- [ ] 7.6 `pnpm --filter @yapm/docs build` passes

## 8. Verify the argument, not just the build

- [ ] 8.1 Re-run the delta-hazard grep before archiving, exactly as PROCESS.md §1 prescribes: `grep -rl "Requirement: The browser-facing sync origin is runtime configuration" openspec/changes/*/specs/`. At authoring time it returned nothing — no in-flight change claims it, so `openspec/specs/self-host-deploy/spec.md` is the baseline and **no archive order is owed to any sibling**. Four changes were in flight and a fifth (`delivery-legibility`) appeared mid-authoring, so re-run it rather than trusting this line
- [ ] 8.2 Walk every scenario in `openspec/changes/config-wait/specs/self-host-deploy/spec.md` and confirm each is true of the built product. The five carried scenarios must still hold — this change is not allowed to break the no-rebuild guarantee, and the two that prove it (`Changing the served origin…`, `No build-time sync origin…`) are covered by `runtime-config.test.tsx:72-84` and `:176-195`
- [ ] 8.3 **By hand against a live stack, the hung case** — the measurement this change exists to move. Suspend the API server with `SIGSTOP` (not `kill`: a killed server refuses connections and produces the *other* distribution), load the app, and time it. Expect nothing at 0–1s, `Loading…` drawn from ~1s, and the named failure at ~15s. Record the reading in `design.md` under "Decisions made during implementation" beside D1's derivation
- [ ] 8.4 **By hand, the refused case.** Stop the API server outright and load the app. Expect the *same* 15s, not the 0–31s spread D1 derives for `main`. This is the half the original hand check never ran, and it is the one that proves the counter was a proxy
- [ ] 8.5 **By hand, the healthy case.** Load the app against a working stack and confirm **nothing is drawn at all** — no flash, no line, no layout shift before the first real surface. This is the risk this change carries, and it is the one thing no test in §5 can rule out convincingly
- [ ] 8.6 **By hand, with a screen reader.** Confirm the announcement still fires once at the beat and that the visible line and the announced text are the same words at the same moment. The whole point is that the two readers stop disagreeing; a fix that draws the line but breaks the announcement has swapped the defect rather than removed it
- [ ] 8.7 **By hand, in dark mode and in at least one non-default preset.** The gate renders above `ThemeProvider`, so the theme comes from the inline bootstrap at `apps/web/index.html:7-36`. Confirm the line is legible in both, which is what `contrast.test.ts` asserts and what only an eye can confirm on a surface no test renders
- [ ] 8.8 Confirm the composed worst case: with the API server suspended, a caller holding a session reaches something they can press in ~15s rather than the ~106s that `front-door`'s bounded landing wait stacked on top of this gate produced. Note the reading beside `front-door/design.md:745-780` in this change's implementation log — that section asked the question

## 9. Gates

- [ ] 9.1 `pnpm turbo lint typecheck test build` green
- [ ] 9.2 The full Playwright suite green: `pnpm --filter @yapm/web e2e`, the invocation CI uses
- [ ] 9.3 `npx -y @fission-ai/openspec@latest validate --all` green
- [ ] 9.4 The compose smoke test green. `scripts/smoke.mjs:76` loads the app against a healthy stack and waits on `connection-status`; this change must draw nothing on that path (§8.5's case, mechanically)
- [ ] 9.5 `ROADMAP.md` is deliberately **not** edited on this branch (`SCOPE-legibility.md:191-193` — siblings are authored in parallel and that file is the guaranteed conflict). Confirm the integrator's row describes what you built, and flip only its status column at archive time
- [ ] 9.6 Record every decision taken during the build in `design.md` under "## Decisions made during implementation", including the three hand-check readings from §8.3, §8.4 and §8.8
