## 1. Read the rulebook first

- [x] 1.1 Read `openspec/SCOPE-legibility.md` in full — the complaint, the eight maintainer answers, and §"Track D — the destination budget". This change is D1 and only D1. Note that `D1` names a different change in `openspec/SCOPE-planning-surfaces.md:96` (`status-history`); do not conflate them
- [x] 1.2 Read `openspec/specs/app-frame/spec.md` end to end. `:45-94` is the requirement this change removes and replaces, and `:179-212` ("Honest degradation where no team is in context") is the one it **modifies** — that requirement names the old count three times (`:181`, `:184`, and its first scenario's THEN at `:194`), so it travels with the replacement or the archived capability contradicts itself. `:20-21` (the deck never adapts), `:169` (zero is absence) and `:259-267` (no route unreachable) are **not** modified and every one of them is load-bearing in the argument. The capability's Purpose line at `:6` ("It carries the six destinations") names it a fourth time and no delta can carry it — see task 8.10
- [x] 1.3 Read `openspec/changes/destination-budget/design.md` in full before touching a file. D4 and D5 are the two decisions a reviewer will push on, and D11 is the obligation nobody remembers until the mocks contradict the product
- [x] 1.4 Read `apps/web/src/frame/deck.tsx` in full — the header comment at `:18-24`, `Destinations` at `:98-151`, the fold-order comment at `:153-154`, and both menu groups (`:169-201` responsive, `:202-241` permanent). Notice that only the permanent group's items carry an explicit `aria-current` (`:209`, `:222`, `:234`) and the responsive group's do not
- [x] 1.5 Read `apps/web/src/frame/go-to.ts` in full and confirm for yourself that **no case in the switch at `:35-71` changes in this task list.** If a task later asks you to edit it, the task is wrong (design D9)
- [x] 1.6 Read `openspec/changes/decision-record/specs/app-frame/spec.md:5-10` and `:17-19`, **and `specs/team-home/spec.md:33-40`** — that delta is not `## ADDED` only: it restates the digest requirement wholesale from the pre-B1, pre-D1 text, so it is the third change queued against the one requirement B1 and this change both rewrite. Do not edit that change's files at any point in this build; design D8 states both consequences in prose and D14 states the revert mechanism, and that is the whole of it
- [x] 1.7 Read `openspec/changes/explanation-at-rest/specs/team-home/spec.md` in full. It modifies the **same** requirement this change's `team-home` delta modifies, and an OpenSpec `## MODIFIED` block restates a requirement wholesale — so that file, not `openspec/specs/team-home/spec.md:8-20`, is this delta's baseline. Confirm for yourself that this change's delta carries B1's composition-record clause, its "no explanatory prose at rest" paragraph and its "The page states no derivation at rest" scenario through verbatim (design D14). Edit no file of B1's

## 2. The deck (`apps/web/src/frame/deck.tsx`)

- [x] 2.1 Remove the Triage `<Link>` from the bar (`:123-130`). Home, Issues, Cycles, Delivery stay in that order; Home keeps `activeOptions={{ exact: true }}` (`:108`) and its reason (`:101-104`)
- [x] 2.2 Move Triage's menu item out of the responsive `Team` group (`:171-179`) into the permanent `More` group (`:202-241`), as its **first** item, and give it the explicit `aria-current={current(stop === 'triage')}` its new siblings already carry at `:209`, `:222` and `:234`. Drop its `className="sm:hidden"` (`:172`) — the permanent group is drawn at every width, which is what makes `g t` advertised at every width (design D9)
- [x] 2.3 The `Team` group (`:169-201`) keeps Cycles (`:180-188`, `md:hidden`) and Delivery (`:189-200`), and keeps its own `lg:hidden`. Cycles' and Delivery's bar links keep `hidden md:flex` (`:135`) and `hidden lg:flex` (`:144`) unchanged — the fold order is unchanged, there is simply one less thing to fold
- [x] 2.4 Rewrite the header comment at `:18-24` and the fold comment at `:153-154`: both state the old membership in prose. Say what the deck now offers, in which tier, and **why Triage sits where it does** — a constraint the code cannot express, which is the only kind of comment `CLAUDE.md` permits. Two more comments in the same file state the old count and belong to the requirement this change modifies: `:60` ("Where the six stops point") and `:81-82` ("A workspace with no teams at all drops the six stops"). A grep of `apps/web/src/frame` for `six stops` finds two more outside this file — `team-context.ts:18` and `team-context.test.ts:9` — and both say the old membership too
- [x] 2.5 `DeckStop` (`:26-34`) is unchanged. It already carries eight members, which is itself the evidence that the "six stops" sentence was never counting destinations

## 3. Nothing else in the frame moves

- [x] 3.1 `apps/web/src/frame/go-to.ts`: **no case changes.** Update only the comment block at `:5-11`, which names the grammar the deck advertises, so it says where each destination is drawn. `PREFIX_WINDOW_MS` (`:12`) and the four guards (`:19`, `:22-24`) are untouched
- [x] 3.2 `apps/web/src/frame/app-frame.tsx:120-183`: the palette's `Go to` group keeps all eight commands, in the same order, with the same `shortcut:` strings. Confirm by reading that every string still matches `go-to.ts` — that agreement is now a spec requirement, not a nicety
- [x] 3.3 `apps/web/src/frame/app-frame.tsx:58` (`useGoTo`) and `:68` (the `current` prop passing) are unchanged. No route file changes: the eleven files passing `current=` keep passing exactly what they pass today, `teams.$teamId.triage.tsx` included

## 4. Home's onward footer (`apps/web/src/home/team-home.tsx`)

- [x] 4.1 `OnwardFooter` (`:976-1032`): keep the Board link (`:992`) and the `⌘K` hint (`:1024-1029`); remove Issues (`:985`), Delivery (`:1000`), Retro (`:1011`) and Roadmap (`:1018`) with their dividers. The call site at `:128` is unchanged (design D10)
- [x] 4.2 **Do not touch a band's own doorway.** The SHIP CADENCE band's link to Delivery, the hero's artifact chips, exception rows, YOURS rows, READY FOR YOU rows and the Members doorway all stay. If a band folds differently after your edit, you edited the wrong thing
- [x] 4.3 `packages/schema/src/zero/team-home.ts` is **not** edited. The footer is a rendering decision; no model field, no footline clause and no derivation changes
- [x] 4.4 **The two other doorway feet are left whole** (design D13). `apps/web/src/triage/triage-view.tsx:1004-1031` (`EmptyQueue` — `Issues` `:1019`, `Cycles` `:1021`, `Projects` `:1023`) and `apps/web/src/notifications/inbox-view.tsx:498-519` (`EmptyInbox` — `Issues` `:513`, `Home` `:515`) draw deck destinations at their feet and are **not** edited by this change: both are required by capabilities this change does not own (`openspec/specs/triage/spec.md:241`, `openspec/specs/notifications/spec.md:668`), and the requirement in §specs reaches a surface that has shown its own content, not an empty state. If a diff of this branch touches either file, the edit is wrong
- [x] 4.5 Grep `apps/web/src` for `Doorway` and confirm the audit is still complete — three files today: `team-home.tsx`, `triage-view.tsx`, `inbox-view.tsx`. A fourth appearing before this change lands is a surface D13 has not costed, and it needs the same two-line judgement before the requirement ships

## 5. The enforcement, because a budget nothing enforces is a comment

- [x] 5.1 `apps/web/src/routes.test.tsx`: `ROUTE_HOMES` (`:132-159`) records `'/teams/$teamId/triage': 'stop'`. Change it to `'more'` — and then close the hole that let it be wrong: today only the table's **keys** are compared against the router (`:171`), so the `'stop' | 'more' | 'doorway'` values are never checked against the deck at all. Add an assertion that every `'stop'` route is a bar link in the rendered deck and every `'more'` route is an item in the menu's permanent list
- [x] 5.2 Same file: add the ceiling as a test — the number of routes whose home is `'stop'` or `'more'` is at most eight, and at most four are `'stop'`. This is the assertion a ninth destination trips over, and it belongs beside `ROUTE_HOMES` because that table is the only enumeration of the deck's membership that exists outside JSX
- [x] 5.3 Do **not** add a lint rule, a codegen step or a config file to hold the deck's membership. `deck.tsx` staying hand-written JSX is a deliberate property of this codebase; the test is the guard

## 6. Unit and component tests

- [x] 6.1 `apps/web/src/frame/app-frame.test.tsx:272-285`: the test named `'the deck offers the six destinations and nothing else'` asserts `toEqual(['Home', 'Issues', 'Triage', 'Cycles', 'Delivery'])` at `:281`, with the comment "The sixth is `more▾`" at `:282` and `const more = …` at `:283`. Rename it for what it now proves, assert the four bar destinations, and keep the two `more` assertions (`:284-285` — a button, never `aria-current`) verbatim. The comment block above the test (`:269-271`) opens "The six destinations of §"Destinations", in the northstar's order" and states the old fold order, so it moves too
- [x] 6.2 Same file: a new case for the menu's permanent list — opening `more▾` offers Triage, Retros, Projects and Roadmap, each with its keyboard hint, at the test's default width. This is the assertion that fails if somebody puts Triage back in the `lg:hidden` group
- [x] 6.3 Same file: a new case for the current-page marking on a menu destination — on `/teams/team-1/triage` the menu's Triage item carries `aria-current="page"`, the trigger carries none, and exactly one element in the navigation landmark claims the current page. **Nothing asserts this today**, for any menu destination
- [x] 6.4 Same file: confirm `:260-266` (one current destination on `/issues`) stays green **without edits**, and read the comment at `:270-271` before believing any test about folding — jsdom applies no stylesheet, so a responsive class is invisible to every test in this file. That is why 6.2 asserts membership rather than visibility
- [x] 6.5 `apps/web/src/home/team-home.test.tsx:487-517`: the onward-footer contract asserts `Issues`, `Board`, `Members`, `Retro` and `Roadmap` links. Keep `Board` (`:495`) and `Members` (`:496-499`); remove the `Issues` (`:491-494`), `Retro` (`:500-503`) and `Roadmap` (`:504-507`) assertions. Keep the accessible-name sweep at `:509-516` exactly as it is — it is the guard that the surviving doorway is still named. `Members` is not a footer link at all (it is the digest's own Members doorway, required by `openspec/specs/team-home/spec.md:298-305`), which is why it survives a change to the footer
- [x] 6.6 Same file, the off-team cases: `:330`'s comment ("would otherwise leave six stops pointing at" a team the caller lost) and `:454`'s ("A workspace with no teams drops the six stops") both state the count the modified degradation requirement no longer fixes. Reword both; the assertions under them are unchanged, because what those tests prove — the anchor resolves, and a caller with no team is offered no destination — is exactly what the modified requirement still says
- [x] 6.7 Confirm green **without edits**: `apps/web/src/frame/command-registry.test.tsx` (the ⌘K registration contract — the frame's `Go to` group is unchanged in content and order, per task 3.2), `apps/web/src/frame/sync-indicator.test.tsx` and `apps/web/src/frame/masthead.test.tsx` (band 1's neighbours, untouched), `apps/web/src/triage/triage-view.test.tsx` (nothing in the Triage view changes), `apps/web/src/notifications/inbox-view.test.tsx` (nothing in the inbox changes), and every `packages/schema` test — this change touches no schema file at all. The two empty-state suites are the guard on task 4.4: if either goes red, something edited a foot the change deliberately left whole. There is **no** `apps/web/src/frame/statusline.test.tsx`; `statusline.tsx` is covered through `app-frame.test.tsx` and `sync-indicator.test.tsx`

## 7. E2E

- [x] 7.1 `apps/web/e2e/support.ts`: `stop()` (`:69-71`) and `goToMore()` (`:81-91`) both survive unedited — the helpers are already the right shape. Fix the two stale comments above them: `:66-68` says "The deck's six destinations", and `:73` says "Retros, Projects and Roadmap live behind `more▾`". The second sentence of `:66-68` ("Home's onward footer links to Issues too") is also about to stop being true, and it is the *reason the helper is scoped to the landmark* — keep the scoping, fix the example
- [x] 7.2 `apps/web/e2e/triage.spec.ts:78-82` (`openTriage`) uses `stop(page, 'Triage').click()` at `:79`; it becomes `goToMore(page, 'Triage')`. Read `goToMore`'s comment at `:73-80` before assuming a plain click works — the trigger is clickable before the transient can respond, which is why that helper retries on `aria-expanded`
- [x] 7.3 Same file `:269`: the viewer case does the same click on a second browser context and needs the same change. Its comment at `:262-264` must be rewritten with it — "The Triage link lives on the issue-views switch, so reach it via Issues (the team overview only links to Issues and Board)" describes a switch this product does not have and is stale **today**, before anything in this change lands; after the move it is stale twice over. Rewrite it to say what the viewer is actually doing, and while you are there decide whether the `stop(vp, 'Issues')` click at `:268` — which exists only because that sentence claimed Triage hung off Issues — still earns its place. Leave the rest of that test alone
- [x] 7.4 Same file: `:76-77` comments that the heading is `Triage` exactly because the team "is two stops to the left in the deck" — reword, and confirm `sendToTriage` (`:62-74`, the palette path) is untouched, because the palette is still the only producer and this change did not build one
- [x] 7.5 Grep `apps/web/e2e` for `stop(` and confirm no other spec reaches Triage through the bar. Confirm `projects.spec.ts:129`'s `goToMore(page, 'Roadmap')` and every other `goToMore` call still passes — the menu gained an item, it did not lose one
- [x] 7.6 New e2e coverage in `apps/web/e2e/triage.spec.ts`: `g t` from a team surface opens Triage without touching the bar or the menu — the assertion that proves a member who learned the old deck lost nothing (design D9)
- [x] 7.7 New e2e coverage: opening `more▾` at the default 1280px viewport shows Triage with a `g t` hint. Today that hint is invisible at this width because the whole `Team` group is `lg:hidden` (`deck.tsx:169`), so this test fails before the change and passes after — which is the cheapest proof the advertisement genuinely improved

## 8. Documentation

- [x] 8.1 `apps/docs/src/content/docs/features/app-frame.md`: the frontmatter description (`:3`), the deck row of the band table (`:11`), §"The six destinations" — its heading (`:18`), the sentence at `:20`, the bold stop list at `:22`, the `more▾` sentence at `:24-25` and the fold paragraph at `:27-30`. State the two tiers, the ceiling, and that the count is over destinations
- [x] 8.2 Same file: the reach table (`:34-42`) gains no row — no route moved — but say plainly, near it, what earns a seat and what a new destination costs. This page is where a contributor will look for the budget before they find the spec
- [x] 8.3 Same file: the keyboard table (`:91-101`) is **unchanged in content**, and say why in the prose around it — a binding belongs to its destination, and moving Triage did not move `g t`
- [x] 8.4 Same file, §"Off a team" (`:71-85`): `:76` ("the six stops stay, pointing at your **anchor team**") and `:81` ("a workspace with no teams at all drops the six stops entirely") are this page's telling of the requirement this change **modifies**, so they carry the same generalisation — the deck's destinations, in whichever tier each sits, however many the budget allows. The rule underneath at `:84-85` is unchanged and still true
- [x] 8.5 Same file, `:106-112`: the ⌘K paragraph lists the frame palette's contents as "the six destinations, the inbox, search everything, appearance" — one clause, one number, and the palette is exactly where the count must be right, because the binding rule makes the palette the place every destination advertises its key
- [x] 8.6 `apps/docs/src/content/docs/features/triage.md:11` — "take the **Triage** stop in the deck — `g t`" — says where it is. Update the placement, keep the shortcut, and do not turn one clause into a paragraph explaining a demotion; the word diet applies to the docs' own sentences too. `:14` — "It does not repeat the team name: the deck already carries it two stops to the left" — is nonsense once Triage is not on the bar at all: the reason survives (the deck names the team), the counting does not
- [x] 8.7 `apps/docs/src/content/docs/features/delivery.md:8` — "is one of the six stops in [the deck](/features/app-frame/) — `g d`" — and `apps/docs/src/content/docs/index.md:39` — "Six destinations, one command palette" — are each one clause naming the old count on a page this change otherwise does not touch. Neither is a place to explain the budget; both just stop being wrong
- [x] 8.8 `apps/docs/src/content/docs/features/team-home.md`: the onward footer's contents (design D10)
- [x] 8.9 `DESIGN.md:23` — "The deck's six stops — Home · Issues · Triage · Cycles · Delivery · `more▾` … Below the deck's comfortable width the stops fold into `more▾` from the right (Delivery, then Cycles, then Triage)". Both halves are now wrong. `README.md:63-65` names the six destinations in the same shape (`:62` is blank)
- [ ] 8.10 **At archive time, by hand:** `openspec/specs/app-frame/spec.md:6` — the capability's Purpose reads "It carries the six destinations, the one attention number, and the single owner of the command-palette shortcut". **No delta can carry this line**: OpenSpec's archive step rewrites only the target spec's `## Requirements` section and copies everything above it — `## Purpose` included — through untouched, so a `## Purpose` block in a delta is honoured only when the capability is being created (design D1). Edit it in the same commit that archives this change, or the capability ships stating a count no requirement in it still makes
  > **Not done, and deliberately: this is the archive-time hand edit.** The integrator takes
  > `openspec/specs/app-frame/spec.md:6` in the commit that archives this change.
- [x] 8.11 `pnpm --filter @yapm/docs build` passes

## 9. The northstar redraw (design D11 — this change owns it, per `SCOPE-legibility.md:107-111`)

- [x] 9.1 Remove the Triage `gb-tab` span from all eight files: `home.html:194-199`, `issues.html:195-200`, `issue.html:277-282`, `delivery.html:173-178`, `ia.html:255-260`, `home-digest.html:244-249`, `home-digest-2.html:352-357`, `home-digest-2-quiet.html:244-249`. Edit all eight or none — `NORTHSTAR.md:40-41` asserts one `md5` across the canonical set, and a partial edit turns a verified invariant into a false claim
- [x] 9.2 `ia.html`, three more drawings: the frame miniature (`:283-284`), the headline "Six stops on the bar." (`:320`), and the destination tree — Triage's root at `:343` moves under the `more ▾` node at `:359` beside Retros, Projects and Roadmap
- [x] 9.3 `ia.html:372-375` draws the `more▾` menu open with four `kbd` hints, including `Decisions … g d` at `:375`. Add Triage's `g t`. **Leave the Decisions row drawn** and record it as a divergence in 9.5 rather than deleting it — that is how `NORTHSTAR.md:65-67` already handles the same item's first fold
- [x] 9.4 The three digest onward footers — `home-digest.html:420-426`, `home-digest-2.html:633-639`, `home-digest-2-quiet.html:423-429` — carry `Issues · Delivery in full · Retro · Roadmap` plus the `⌘K` hint, and **no board link at all**. Rationing them to match D10 is therefore two edits, not one: remove those four spans with their `·` dividers, **and add a `Board` doorway** in the same `<span>…<span class="c">›</span></span>` shape before the hint. Removal alone leaves a foot holding a `⌘K` hint and nothing else, which contradicts the delta's "the onward footer SHALL carry only the surfaces the deck does not itself offer as destinations — today the board". Two of the three already draw a `Board` doorway elsewhere on the page — an exception row's `where` chip (`home-digest.html:295`, `home-digest-2.html:432`) — and that is a band's own doorway, untouched by D10 and not the footer's; the footer's board link is new ink in all three files
- [x] 9.5 `NORTHSTAR.md`: correct the "six stops with `more▾` as a transient" sentence (`:56-57`); re-verify and re-state the `gbar` md5 at `:40-41` (today `571eee83506c`, computed over the normalized `<header class="gbar">` modulo the active-tab class); and add a **fourth divergence** beside the three at `:61-83`, in the same voice — what the mocks draw, what the product does, and which is right
- [x] 9.6 Do **not** re-render the PNGs as a repository task. `.gitignore:55` is `design-explorations/**/*.png` and `git ls-files design-explorations/overhaul-2026-08/northstar/` returns nine files — `NORTHSTAR.md` and the eight HTML. There is no tracked screenshot to update and no reviewer who would see one
- [x] 9.7 `design-explorations/overhaul-2026-08/destinations/`, `plays/` and `horizon/` also draw this deck (a repo-wide grep for `gb-nav` finds 38 HTML files). `destinations/DESTINATIONS.md:9` calls those candidates rather than canonical, so they are **out of scope** — state that decision in the `NORTHSTAR.md` annotation so the next reader does not re-open it

## 10. Verify the argument, not just the build

- [x] 10.1 Walk every scenario in `openspec/changes/destination-budget/specs/**` and confirm each is true of the built product. The three that need a real browser rather than a test: "A shed destination is not offered twice", "A menu destination advertises its key at every width", and "Two mornings read the same"
- [x] 10.2 By hand, at three widths (1440, 900, 600): open the deck and the menu at each, count the destinations offered, and confirm the number is eight every time and that nothing is listed twice. This is the ceiling's own derivation (design D2) and it is not observable in jsdom
- [ ] 10.3 By hand, on a team with an empty triage inbox, an empty retros list and no projects: confirm every destination is still offered in the same tier and order, and that each opens onto its own empty state (design D4)
  > **Done in part.** The running stack's team has an empty triage inbox (`Nothing waiting.`,
  > with its foot whole) but has retros and projects, so the three-way empty case was not
  > observed. See `design.md` §"Decisions made during implementation".
- [x] 10.4 Confirm `openspec/specs/delivery-metrics/spec.md:226` ("or by its `g d` shortcut") is still true of the built product and was not edited by this change. It is the canary for design D9
- [x] 10.5 Re-read `openspec/specs/app-frame/spec.md:259-267` and confirm no route lost its home. Triage moved tier; it did not lose reachability, and `ROUTE_HOMES` should say so
- [x] 10.6 By hand, both halves of the second-navigation rule: Home's foot offers the board and the `⌘K` hint and no deck destination; a team with a cleared triage queue still offers `Issues · Cycles · Projects` and an empty inbox still offers its two doorways. Both scenarios are in the delta, and the second one is the audit D13 argues — a build that "tidied" it has broken `openspec/specs/triage/spec.md:252` and `openspec/specs/notifications/spec.md:688`

## 11. Gates

- [x] 11.1 `pnpm turbo lint typecheck test build` green
- [ ] 11.2 The full Playwright suite green: `pnpm --filter @yapm/web e2e`, the invocation CI uses. Run it twice — §7 changes a helper two specs share, and helper breakage is order-dependent
  > **Not run — cannot be run against the dev stack on this machine, and not claimed.** The
  > suite boots its own app server on a second port; zero-cache calls back into the DEV server,
  > which verifies the sync JWT against its own issuer, so every spec times out at sign-in before
  > reaching an assertion. Root cause isolated by hand; see `design.md`. CI owns this gate.
- [x] 11.3 `npx -y @fission-ai/openspec@latest validate --all` green
- [ ] 11.4 The compose smoke test green
  > **Not run.** Port 3000 is held by an unrelated container on this machine, which is why the
  > compose smoke test cannot run locally. CI owns it.
- [x] 11.5 `ROADMAP.md` is deliberately **not** edited on this branch (`SCOPE-legibility.md:191-193` — siblings are authored in parallel and that file is the guaranteed conflict). Confirm the integrator's row describes what you built, and flip only its status column at archive time
- [x] 11.6 **Merge order, before archiving** (design D14): confirm B1 `explanation-at-rest` has archived and its text is live in `openspec/specs/team-home/spec.md` — the band-order sentence naming the composition record behind a `how ·`, and the "no explanatory prose at rest" paragraph. This change's `team-home` delta is the union of B1's text and this change's rationing, so syncing it over the pre-B1 text loses B1's clauses. If B1 has **not** archived, stop and say so rather than archiving out of order: the repair is to re-author one of the two deltas, not to land both. The same requirement has a **third** claimant — `decision-record/specs/team-home/spec.md:33-40`, still holding the pre-B1, pre-D1 text — which is parked and must not archive after this change without being re-authored as the union first (design D8, D14). Nothing here edits it; note it in the hand-off so the next person does not archive a revert
- [x] 11.7 Record every decision taken during the build in `design.md` under "## Decisions made during implementation"
