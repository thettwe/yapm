## 1. Read the rulebook first

- [ ] 1.1 Read `openspec/SCOPE-legibility.md` in full — the complaint, the four maintainer answers, and §"Track C — the front door". This change is C1 and only C1
- [ ] 1.2 Read `openspec/specs/app-frame/spec.md` §"Honest degradation where no team is in context" (:179-212) and §"No route is unreachable from the frame" (:259-286). Neither is modified by this change; confirm for yourself that neither is contradicted either
- [ ] 1.3 Read `openspec/specs/issue-list/spec.md` §"Status-grouped keyboard-first issue list" (:6-25) and §"Filtering, sorting, and saved views" (:111-190) — the first is deltaed, the second deliberately is not
- [ ] 1.4 Read `apps/web/src/frame/team-context.ts` in full, especially the header comment at :13-19 (why the deck may point where the reader is not) and `resolveAnchorTeam` at :49-59
- [ ] 1.5 Read `apps/web/src/components/authenticated.tsx:22-30` — the two-routes-Navigating-at-each-other starvation this change must not build (design D4)
- [ ] 1.6 Read `reference/*.md` for better-auth before touching `login-form.tsx`; where it does not cover the redirect plugin, read the installed package — `node_modules/.pnpm/better-auth@1.6.24_*/node_modules/better-auth/dist/api/routes/sign-in.mjs:335-341`, `dist/api/routes/sign-up.mjs:262-265`, `dist/client/fetch-plugins.mjs:3-15`, `dist/client/config.mjs:50`. **Do not write these APIs from memory** (design D1)

## 2. The landing team (`packages/schema` is not involved — this is frame state)

- [ ] 2.1 `apps/web/src/frame/team-context.ts`: export `resolveLandingTeam(teams, remembered, viewer)` beside `resolveAnchorTeam`, over the same `FrameTeam` shape plus the `members` relation `queries.teams.all()` already carries (`packages/schema/src/zero/schema.ts:671-676`, `userId` at `:82`). Order: the remembered team when the viewer can read it → the first readable team → `null` (design D2)
- [ ] 2.2 Same file: "can read" is `viewer.role === 'admin'` (mirroring `teamScoped`'s own bypass at `packages/schema/src/zero/queries.ts:26-28`) or a `team.members` row whose `userId` is the viewer's. No new query, no new sync
- [ ] 2.3 **Do not touch `resolveAnchorTeam` (:49-59) or `useAnchorTeam` (:61-77).** The deck's behaviour is `app-frame/spec.md:181-212` and it does not move in this change. Write the reason as a file comment, not as a commit message

## 3. One landing decision (`apps/web/src/routes/login.tsx`)

- [ ] 3.1 `login.tsx`: replace `return <Navigate to="/" />` at `:21` with the resolved landing navigation — `/teams/{teamId}` when `resolveLandingTeam` returns a team, `/` when it returns `null`
- [ ] 3.2 Same file: gate the decision on `useSyncSession().status === 'ready'` **and** `teamsResult.type === 'complete'` (the `team-home.tsx:56` / `:106-112` precedent), rendering the `Loading…` node that already exists at `:10-18` while either is outstanding. A settled `role === null` falls to `/`, where the access gate renders (design D3). Neither condition alone is enough: an unidentified caller's `queries.teams.all()` resolves through `denyAll` (`packages/schema/src/zero/queries.ts:114`, `:13-19`) and reports **complete-and-empty**
- [ ] 3.3 Same file: the wait needs an end, because `ready` is not the only settled state (`apps/web/src/zero/provider.tsx:40` declares three, and `unavailable` at `:54` is carried beside them). Check `unavailable` **before** `ready` — it coexists with `pending` — and render `SyncUnavailable`; on a clean settled `logged-out`, render the `<LoginForm />` node `:24-28` already has. **Navigate in neither case**: `apps/web/src/components/authenticated.tsx:22-30` sends a clean `logged-out` straight back to `/login`, which is D4's starvation (design D3)
- [ ] 3.4 `apps/web/src/components/authenticated.tsx`: export `SyncUnavailable` (`:52-67`) so `/login` renders the same node, the same `data-testid="sync-unavailable"` and the same `retry` button rather than a second copy — `e2e/reconnect.spec.ts:340-341` and `:369-372` drive that testid. Change nothing about the component or about `Authenticated`'s own branches at `:16-31`
- [ ] 3.5 Confirm by reading, not by assuming, that `main.tsx:30-36` mounts `ZeroRoot` above `RouterProvider` so `useQuery` is legal on `/login`; if that ever stops being true this task is wrong

## 4. The other landing mechanisms nobody named (`login-form.tsx`, then `invite.tsx`)

- [ ] 4.1 Drop `callbackURL` from the two email calls — `signIn.email` at `:54` and `signUp.email` at `:59` — so better-auth returns `redirect: false` and its `redirectPlugin` cannot fire a `window.location.href` landing that competes with task 3 (design D1)
- [ ] 4.2 Point the two provider calls at the one decision point: `CALLBACK_URL` at `:19` becomes `'/login'`, kept on `signIn.social` (`:78`) and `signIn.sso` (`:94`), because a third-party redirect structurally needs a URL. `ROUTE_HOMES`'s `'/login': 'open'` (`routes.test.tsx:157`) is unchanged
- [ ] 4.3 Leave the form's error handling, provider gating and `login-submit` testid exactly as they are — fifteen e2e specs drive them
- [ ] 4.4 Read `apps/web/src/routes/invite.tsx` before editing it, and note that `LoginForm` is mounted **here too** (`:57`) as well as on `login.tsx:26` — 4.1 and 4.2 both land on this page. Confirm from `apps/web/src/zero/provider.tsx:477-484` that a changed `authUserId` re-mints the credential, which is what lets a no-`callbackURL` email sign-in stay on `/invite?token=…` and actually accept the invitation (design D10)
- [ ] 4.5 `apps/web/src/routes/invite.tsx:97-99`: the third landing mechanism. Replace `void navigate({ to: '/' })` with the same landing resolution — read `teamId` off the accept response (`apps/server/src/auth-routes.ts:203-208` already returns it; `:87-89` currently reads the body only on failure) and navigate to `/teams/{teamId}` when it is present; otherwise fall to `resolveLandingTeam` under the same gate as task 3. **Do not edit `auth-routes.ts` or `packages/schema/src/db/invite.ts`** — `:118-128` already writes the `team_membership` row and `:139-145` already returns the id
- [ ] 4.6 Leave the invite page's other states alone — the missing-token branch (`:30-39`), the `pending` and `logged-out` branches (`:41-60`), the error copy at `reasonText` (`:19-24`) and `BackHome`'s link to `/` (`:133`), which is a deliberate escape hatch and not a landing

## 5. The default lens (`packages/schema`, then `apps/web`)

- [ ] 5.1 `packages/schema/src/zero/filter.ts`: `TERMINAL_ISSUE_STATUSES = ['done', 'canceled']` and `DEFAULT_ISSUE_STATUS_FILTER` **derived** from `ISSUE_STATUSES` (`packages/schema/src/zero/context.ts:9-16`) by exclusion, never listed literally (design D5). No change to `IssueFilter` (`:30-38`), `issueFilterSchema` (`:58-65`) or `matchesFilter` — the default is a value the axis at `:31` already accepts and the evaluator at `:86` already reads
- [ ] 5.2 Export both from `packages/schema/src/index.ts`
- [ ] 5.3 `apps/web/src/issues/issue-list.tsx:210`: seed `useState<IssueFilter>` from `DEFAULT_ISSUE_STATUS_FILTER` instead of `{}`. Nothing else on the surface changes — not the grouping, not the sort, not the fold, not the masthead count expression
- [ ] 5.4 `apps/web/src/board/board.tsx:269`: **leave `useState<IssueFilter>({})` alone**, with a comment naming why — the board's columns are the status axis (`apps/web/src/board/model.ts:30` maps `STATUS_ORDER` unconditionally) and a card dragged into a filtered-out Done column would vanish (design D6)

## 6. Unit and component tests

- [ ] 6.1 `apps/web/src/frame/team-context.test.ts`: new cases for `resolveLandingTeam` appended beside the existing anchor suite — a readable remembered team wins; a remembered team the viewer is not in falls through; a non-member of `teams[0]` gets the first team they are in; an admin gets the anchor; a member of no team gets `null`; an empty workspace gets `null`. **Do not edit the existing cases at :40-42, :44-46, :50-52, :54-56, :59-62, :64-70 or :75-90** — they are the guard that proves the deck did not move
- [ ] 6.2 `apps/web/src/components/auth/login-form.test.tsx`: three assertions carry the old callback and must move — `:74` and `:102` (`signInSso` called with `callbackURL: '/'`) become `'/login'`, and `:85-89` (`signInEmail` called with `callbackURL: '/'`) drops the key entirely. Add a case asserting `signUpEmail` is called with **no** `callbackURL`, because that absence is what stops the second landing (design D1)
- [ ] 6.3 New component coverage for `/login` with a session: a resolved landing team navigates to `/teams/{id}`; no readable team navigates to `/`; an unsettled roster navigates nowhere and renders `Loading…`. **Include the invariant case**: a roster reporting `type === 'complete'` **and empty** while the sync session is still `pending` must render `Loading…` and navigate nowhere — that is the case that fails if a later edit keeps `complete` and drops `ready`, and `denyAll` (`packages/schema/src/zero/queries.ts:13-19`) is why an unidentified caller sees exactly that shape (design D3). `apps/web/src/routes.test.tsx:8-14` shows the `useSession` mock shape to build on
- [ ] 6.4 More `/login` component coverage, for the branches that are settled but not `ready` (design D3): with `unavailable` true — in `pending` and again in `logged-out` — the retry surface renders (`sync-unavailable`) and no navigation is taken; with a clean settled `logged-out` and a live better-auth session, the sign-in form renders and no navigation is taken. The second assertion is the loop guard: a `Navigate` here would meet `authenticated.tsx:30` coming the other way
- [ ] 6.5 New component coverage for `/invite` (design D10): an acceptance whose response carries a `teamId` navigates to `/teams/{teamId}`; one that carries none falls to the same resolution as `/login` — a readable team where one exists, `/` otherwise. There is no `invite` component test today, so this file is new; the accept call is a plain `fetch` (`invite.tsx:75-95`), so the test mocks `fetch` rather than a client. Cover the error and missing-token branches (`:101-110`, `:30-39`) while you are there, since nothing else does
- [ ] 6.6 `apps/web/src/routes.test.tsx`: `ROUTE_HOMES` (`:132-159`) gains and loses nothing — no route is added or removed. Confirm `:161-172` (registered ids equal the table's keys, asserted at `:171`) and `:192-207` (every framed route's file contains `<AppFrame`, read at `:197`) both stay green **without edits**; if either needs an edit, this change has grown a route and task 5 of the proposal's non-goals was violated
- [ ] 6.7 `apps/web/src/issues/issue-list.test.tsx`: the fixture at `:161-167` has `status: 'done'` (`:165`) and two tests read it — `:186` (`phraseOf('Persist cart across sessions')` states `Built — not live yet`) and `:213` (that row carries the GitHub mark). Keep both assertions and make the lens they run under explicit; do not delete the case, because it is the only `merged_not_deployed` phrase fixture in the file
- [ ] 6.8 `apps/web/src/issues/issue-list.test.tsx:425-446`: the seven-axis sweep's Status case at `:426` — `['Status', /Todo$/, 'Alpha row']` — inverts, because clicking `Todo` in a menu whose four values are already seeded toggles it **off**. Rewrite that one case to prove the same property (this axis and no other decides which row survives) through the seeded default; **the other six cases at `:427-432` are untouched**
- [ ] 6.9 `apps/web/src/issues/issue-list.test.tsx`: new cases — the list opens showing only live statuses; the Status axis states its four values; clearing it renders the Done row again
- [ ] 6.10 `packages/schema/src/zero/filter.test.ts`: `DEFAULT_ISSUE_STATUS_FILTER` equals `ISSUE_STATUSES` minus `TERMINAL_ISSUE_STATUSES` **computed in the test**, not compared against a literal four — so a status added to `context.ts:9-16` joins the default rather than being silently hidden (design D5)
- [ ] 6.11 Confirm green **without edits**: `apps/web/src/issues/model.test.ts` (its `base` passes `filter: {}` explicitly at `:18`, so every `buildGroups` call at `:32`, `:54`, `:68`, `:78`, `:92`, `:114`, `:124`, `:134` is unaffected); `apps/web/src/board/board.test.tsx:265` and `:287-291` (all six columns, `'Canceled, 0 issues'`) because the board's default did not change; `apps/web/src/components/header-menus.test.tsx:106` (the switcher's `Workspace` group), because `/` keeps its home

## 7. E2E — the twenty-four helpers that encode the old front door

Every one of these asserts `[data-testid="workspace-name"]` (rendered only by `apps/web/src/components/workspace-name.tsx:88`, only from `apps/web/src/routes/index.tsx:34`) immediately after signing in. On a shared database this lands as **flakiness, not a clean red** — whether a team exists when a spec signs in is not deterministic. Fix them all the same way: sign in, then navigate to `/` explicitly, which is already the pattern at `search.spec.ts:65`, `issues.spec.ts:332`, `attachments.spec.ts:111` and `pm-digest.spec.ts:125`.

- [ ] 7.1 `apps/web/e2e/support.ts`: `ensureAccount` (`:46-64`, its `Promise.race` at `:53-59`) asserts nothing about the landing route and **stays untouched**. `signUp` (`:30-35`) and `signIn` (`:37-41`) likewise. Add the shared "…and open the workspace overview" step here rather than writing it twenty-four times, and export it beside `stop` (`:69-71`) and `goToMore` (`:81-91`)
- [ ] 7.2 The sixteen `enterApp` copies: `auto-status.spec.ts:19-25`, `board.spec.ts:30-36`, `connectors.spec.ts:27-33`, `cycles.spec.ts:28-34`, `digest.spec.ts:26-32`, `issues.spec.ts:25-31`, `mentions.spec.ts:23-29`, `notifications.spec.ts:22-28`, `pm-digest.spec.ts:84-90`, `projects.spec.ts:28-34`, `retro-ai.spec.ts:45-51`, `retro.spec.ts:27-33`, `rich-content.spec.ts:53-56`, `search.spec.ts:55-61`, `sso.spec.ts:24-27`, `triage.spec.ts:23-29`
- [ ] 7.3 The five other post-sign-in helpers: `auth.spec.ts:13-15` (`expectInApp`, called at `:31`, `:40`, `:53`, `:80`, `:90`, `:114`, `:119`), `harness.spec.ts:7-24` (`enterTeam`), `reconnect.spec.ts:35-39` (`openWorkspace`), `sync.spec.ts:16-22` (`openWorkspace`), `theme.spec.ts:7-9` (`expectInApp`, called at `:65` and `:107`)
- [ ] 7.4 The three inline sites: `attachments.spec.ts:103-104`, `zz-isolation.spec.ts:46-47`, and `pm-digest.spec.ts:643-645` — the last is a `signIn` on a fresh context asserting `workspace-name`, so it breaks for the same reason as the helpers
- [ ] 7.5 `auth.spec.ts` **is** the workspace-administration contract and every assertion in it survives verbatim once the helper lands on `/`: `:33`, `:34`, `:35` (members list, Invitations heading, create-team), `:43` and `:48` (create a team, teams list), `:55` and `:59` (create an invite, the link), `:80-81` (the invitee lands and appears), `:92-93` (the viewer sees no admin surfaces), `:95-96` and `:98` (the rename refusal). Do not weaken one of them to make the suite pass. `:80-81` survives for a specific reason worth checking rather than assuming: the invite created at `:55-57` sets a **Role only**, so `invite.team_id` is null and acceptance falls to the ordinary resolution — a brand-new viewer belongs to no team and still lands on `/` (design D10)
- [ ] 7.6 **Do not blanket-rewrite the 22 `page.goto('/')` calls across 13 spec files.** `/` still renders administration, so all 22 keep working — and at least nine of them mean "land somewhere authenticated" rather than "reach administration": `mentions.spec.ts:98-99`, `notifications.spec.ts:146-147` and `:239-240`, `harness.spec.ts:54-56` (a **signed-out** probe expecting the sign-in heading), `retro-ai.spec.ts:238`, and `pm-digest.spec.ts:125-126`, `:254-258`, `:453-457`, `:499-503`
- [ ] 7.7 New e2e coverage in the spec that already owns the entry point (`auth.spec.ts`): signing in with a team of one's own reaches that team's Home rather than administration, and a workspace with no team still reaches administration
- [ ] 7.8 New e2e coverage for the other invitation shape (`auth.spec.ts`): a **team-bound** invite, accepted, lands on that team's Home rather than on administration. The create-invite dialog already offers it — `apps/web/src/components/invites-panel.tsx:260-261` is the "Team (optional)" select and `:195` is where the chosen id goes onto the request. This is the path `:65-82` does not exercise, and the one where `/invite`'s old `navigate({ to: '/' })` was most visibly wrong

## 8. E2E — the two assertions the default lens moves

- [ ] 8.1 `apps/web/e2e/triage.spec.ts:140-143` is the **only** e2e assertion that needs a terminal-status issue to appear in the default Issues lens (a declined issue reappearing under the `Canceled` region). Make the lens explicit; keep the assertion — it still proves that declining sets the status and that the issue is in the list
- [ ] 8.2 Confirm green **without edits**: `triage.spec.ts:179-182` (the routed issue under `Todo` — a live status), and `board.spec.ts:117` (`column(page, 'Canceled')` in the viewport at three widths), which is immune because `apps/web/src/board/model.ts:30` maps `STATUS_ORDER` unconditionally
- [ ] 8.3 New e2e coverage on the Issues surface (`issues.spec.ts`): the list opens without terminal-status rows, and clearing the Status axis brings them back — keyboard-only, per the list's own keyboard requirement

## 9. Documentation

- [ ] 9.1 `apps/docs/src/content/docs/features/app-frame.md`: state where signing in lands and why; extend §"Off a team" (`:71-86`) with the distinction between the deck's **anchor** (an offer, may be a team you are not in) and the **landing team** (a decision, never is). The reach table at `:34-42` (`:34` is its header row, `:42` its last) gains no row — no route was added
- [ ] 9.2 `apps/docs/src/content/docs/features/issue-list.md` §"The filter bar" (`:86-130`): the Status axis opens with four values, what they are, that clearing it restores everything, and that the Board lens starts unfiltered — which `:96` already promises and which is now the second route to the archive
- [ ] 9.3 `apps/docs/src/content/docs/features/team-home.md` and `README.md:92-93`: Home is what you see when you sign in, not what you click through to
- [ ] 9.4 `apps/docs/src/content/docs/features/app-frame.md` again, for the two paths §9.1 does not cover: what an invitation acceptance lands on (design D10), and what the sign-in surface shows when the sync session settles without a usable credential (design D3) — the retry surface the docs already describe elsewhere, never an endless wait
- [ ] 9.5 Confirm nothing else went stale (PROCESS.md §2): `TECHSTACK.md`, `.env.example`, `DESIGN.md` and the self-hosting pages are untouched by this change. `ROADMAP.md` is deliberately **not** edited on this branch — parallel builds collide there — and the debt is not owed to nobody: the integrator has already added row 46 (`ROADMAP.md:65`, `📋 scoped`). Read it, confirm it still describes what you built, and flip only its status column at archive time
- [ ] 9.6 `pnpm --filter @yapm/docs build` passes
- [ ] 9.7 Record every decision taken during the build in `design.md` under "## Decisions made during implementation"

## 10. Gates

- [ ] 10.1 `pnpm turbo lint typecheck test build` green
- [ ] 10.2 The compose smoke test green — `scripts/smoke.mjs:76` does `app.goto(url + '/')` and waits on `connection-status`, and it survives only because a fresh workspace has no team and therefore still lands on administration. If this goes red, the zero-team fallback is wrong
- [ ] 10.3 The full Playwright suite green: `pnpm --filter @yapm/web e2e` (`apps/web/package.json:9`), the same invocation CI uses at `.github/workflows/ci.yml:150`, across all 23 spec files. Run it twice — the helper breakage in §7 is order-dependent, so one green run proves less than usual
- [ ] 10.4 Walk every scenario in `openspec/changes/front-door/specs/**` and confirm each is true of the built surface
- [ ] 10.5 Sign in as a member of no team in a workspace that has teams, and look at what they get. It is administration by design (proposal non-goals; E2 `first-run` owns the better answer) — confirm it is not a team page belonging to strangers
- [ ] 10.6 By hand, with the server stopped after sign-in: load `/login` holding a session and confirm it reaches the retry surface rather than sitting on `Loading…`, and that pressing retry recovers into the landing decision rather than into `/` (design D3). This is the branch no unit test observes end to end and the one a spinner hides
