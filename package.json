{
  "name": "yapm",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.16.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "node scripts/dev.mjs",
    "dev:down": "docker compose -f docker/docker-compose.dev.yml down",
    "dev:reset": "docker compose -f docker/docker-compose.dev.yml down -v",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "e2e": "turbo run e2e",
    "workbench": "pnpm --filter @yapm/ui workbench",
    "lint": "biome ci .",
    "format": "biome check --write .",
    "check:catalog": "node scripts/check-catalog.mjs",
    "check:boundaries": "node scripts/check-boundaries.mjs",
    "check:commit-msg": "node scripts/check-commit-msg.mjs",
    "prepare": "node scripts/install-git-hooks.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@playwright/test": "catalog:",
    "lefthook": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:",
    "yaml": "catalog:"
  }
}
